#pragma once
// GENERATED by Yeroket kernel-codegen (--readmodel-cpp) — DO NOT EDIT. Regenerate from the canonical [View] schema.
#include "UndertowBuildingPower.typed.g.h"   // <Name>Section (FromSection's parameter type)
#include <RmlUi/Core/Types.h>   // Rml::String
#include <cstdint>
#include <vector>

namespace Vixen::Views {

struct UndertowBuildingPowerRowRow {
    float demand;
    float generated;
    float stored;
    float net;
    int connected;
    int impact;
    uint64_t rowId;
};

struct UndertowBuildingPowerReadModel {
    std::vector<UndertowBuildingPowerRowRow> rows;

    static UndertowBuildingPowerReadModel FromSection(const UndertowBuildingPowerSection& s) {
        UndertowBuildingPowerReadModel rm;
        rm.rows.reserve(s.count());
        for (uint32_t i = 0; i < s.count(); ++i) {
            UndertowBuildingPowerRowRow r;
            r.demand = s.demand(i);
            r.generated = s.generated(i);
            r.stored = s.stored(i);
            r.net = s.net(i);
            r.connected = s.connected(i);
            r.impact = s.impact(i);
            r.rowId = s.rowId(i);
            rm.rows.push_back(r);
        }
        return rm;
    }
};

}  // namespace Vixen::Views
