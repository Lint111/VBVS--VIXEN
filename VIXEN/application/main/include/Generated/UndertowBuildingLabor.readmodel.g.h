#pragma once
// GENERATED by Yeroket kernel-codegen (--readmodel-cpp) — DO NOT EDIT. Regenerate from the canonical [View] schema.
#include "UndertowBuildingLabor.typed.g.h"   // <Name>Section (FromSection's parameter type)
#include <RmlUi/Core/Types.h>   // Rml::String
#include <cstdint>
#include <vector>

namespace Vixen::Views {

struct UndertowBuildingLaborRowRow {
    float supply;
    float need;
    int needMet;
    uint64_t rowId;
};

struct UndertowBuildingLaborReadModel {
    std::vector<UndertowBuildingLaborRowRow> rows;

    static UndertowBuildingLaborReadModel FromSection(const UndertowBuildingLaborSection& s) {
        UndertowBuildingLaborReadModel rm;
        rm.rows.reserve(s.count());
        for (uint32_t i = 0; i < s.count(); ++i) {
            UndertowBuildingLaborRowRow r;
            r.supply = s.supply(i);
            r.need = s.need(i);
            r.needMet = s.needMet(i);
            r.rowId = s.rowId(i);
            rm.rows.push_back(r);
        }
        return rm;
    }
};

}  // namespace Vixen::Views
