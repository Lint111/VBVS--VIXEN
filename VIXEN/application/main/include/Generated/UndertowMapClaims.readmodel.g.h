#pragma once
// GENERATED by Yeroket kernel-codegen (--readmodel-cpp) — DO NOT EDIT. Regenerate from the canonical [View] schema.
#include "UndertowMapClaims.typed.g.h"   // <Name>Section (FromSection's parameter type)
#include <RmlUi/Core/Types.h>   // Rml::String
#include <cstdint>
#include <vector>

namespace Vixen::Views {

struct UndertowMapClaimRowRow {
    int body;
    uint64_t claimant;
    int kind;
    int strengthBand;
    int confidence;
    int contested;
};

struct UndertowMapClaimsReadModel {
    std::vector<UndertowMapClaimRowRow> rows;

    static UndertowMapClaimsReadModel FromSection(const UndertowMapClaimsSection& s) {
        UndertowMapClaimsReadModel rm;
        rm.rows.reserve(s.count());
        for (uint32_t i = 0; i < s.count(); ++i) {
            UndertowMapClaimRowRow r;
            r.body = s.body(i);
            r.claimant = s.claimant(i);
            r.kind = s.kind(i);
            r.strengthBand = s.strengthBand(i);
            r.confidence = s.confidence(i);
            r.contested = s.contested(i);
            rm.rows.push_back(r);
        }
        return rm;
    }
};

}  // namespace Vixen::Views
