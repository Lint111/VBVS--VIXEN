
#pragma once
#include <cstdint>
// GENERATED by Yeroket kernel-codegen — DO NOT EDIT. Regenerate from the canonical SDFOpCode enum.
//
// HAND-MIRRORED ADDITION (Recipe-Parameterization-Plan-2026-07 M1 Task 1): `ReadParam`/
// `ReadParamFloat3` below are not yet declared as a `[SdfCoreKernel]`/`[SdfCoreOp]` member on the
// VIXEN side of the Yeroket codegen pass (EmitSdfOpCodeEnum only emits VIXEN's *declared* subset),
// so no working regen path reaches this exact addition in this environment. Verified against
// Yeroket canonical source `Packages/com.utility.graph-framework/Runtime/VM/SDFInstruction.cs`
// lines 143 (`ReadParam = 96`) and 171 (`ReadParamFloat3 = 111`) at Yeroket main `99bce801`
// (2026-07-14). A future real regen pass must mark these `[SdfCoreOp]` in the canonical C# source
// and re-vendor rather than silently dropping this hand-mirror.
namespace Vixen::SVO::Recipe {
enum class SdfOpCode : uint8_t {
    Sphere                 = 0,
    Box                    = 1,
    BoxRounded             = 2,
    Capsule                = 3,
    Cylinder               = 4,
    Plane                  = 5,
    Torus                  = 6,
    Ellipsoid              = 7,
    HollowCylinder         = 8,
    TaperedCylinder        = 9,
    Panel                  = 10,
    Plank                  = 11,
    RoundedBox             = 12,
    CappedTorus            = 14,
    Cone                   = 15,
    RoundCone              = 16,
    FakeRoundCone          = 17,
    Segment                = 18,
    TriangularPrism        = 20,
    Pyramid                = 21,
    HexPrism               = 22,
    Link                   = 23,
    Union                  = 24,
    SmoothUnion            = 25,
    Subtract               = 26,
    SmoothSubtract         = 27,
    Intersect              = 28,
    SmoothIntersect        = 29,
    Xor                    = 30,
    SmoothMax              = 31,
    SmoothUnionCubic       = 32,
    SmoothSubtractCubic    = 33,
    SmoothIntersectCubic   = 34,
    Round                  = 35,
    Onion                  = 36,
    Transform              = 37,
    Elongate               = 38,
    Twist                  = 39,
    Bend                   = 40,
    MirrorX                = 41,
    MirrorY                = 42,
    MirrorZ                = 43,
    RepeatInfinite         = 44,
    RepeatLimited          = 45,
    Revolution             = 46,
    MathSin                = 56,
    MathCos                = 57,
    MathSmoothstep         = 58,
    MathRemap              = 59,
    MathAdd                = 60,
    MathSub                = 61,
    MathMul                = 62,
    MathDiv                = 63,
    MathMin                = 64,
    MathMax                = 65,
    MathClamp              = 66,
    MathAbs                = 67,
    MathFrac               = 68,
    MathPow                = 69,
    MathSqrt               = 70,
    MathLerp               = 71,
    MathNegate             = 72,
    PositionChannel        = 73,
    Displacement           = 74,
    MathStep               = 75,
    MathSign               = 76,
    MathSaturate           = 77,
    MathExp                = 78,
    MathLog                = 79,
    MathLog2               = 80,
    Select                 = 81,
    DistanceTo             = 82,
    Output                 = 94,
    PushParam              = 95,
    ReadParam              = 96,  // hand-mirrored — see file-header note; SDFInstruction.cs:143
    RestorePos             = 97,
    PushFloat3             = 98,
    ComposeFloat3          = 99,
    Passthrough            = 100,
    DecomposeFloat3        = 101,
    Float3Add              = 102,
    Float3Sub              = 103,
    Float3MulComponentWise = 104,
    Float3Min              = 105,
    Float3Max              = 106,
    Float3ScalarMul        = 107,
    Float3Dot              = 108,
    Float3Normalize        = 109,
    // 110 == CurlNoise3D in the canonical enum (NOT free; intentionally not mirrored here — VIXEN
    // has no curl-noise kernel yet, out of scope for this plan).
    ReadParamFloat3        = 111,  // hand-mirrored — see file-header note; SDFInstruction.cs:171

    // HAND-MIRRORED ADDITION (Recipe-Diversity-Stress-Scene-Inc6 M1 — spatial-contract
    // meta/resolve prototype, 2026-07-17): DeclarePosition is NOT a Yeroket canonical opcode
    // at all (no C# SDFInstruction.cs counterpart exists or is planned here) — it is a
    // VIXEN-only, prototype-scoped marker opcode, unlike ReadParam/ReadParamFloat3 above
    // (which mirror a real canonical enumerator not yet marked [SdfCoreOp]). Value 112 chosen
    // as the next free slot after ReadParamFloat3=111; not derived from any canonical source.
    // Semantics: pops a float3 already on the value stack (the meta segment's declared
    // world position, typically produced by ReadParamFloat3) and (a) captures it as the
    // recipe's declared position (exposed via an `out vec3` in the GLSL emitter / an
    // out-param in the CPU eval overload), and (b) folds it into the sample-point position
    // as a pure translation for the remainder of the walk (the resolve segment), so the
    // declared position actually moves where the resolve segment's shape renders — not just
    // a disconnected reported value. See SdfRecipeEval.h/SdfRecipeCodegenGlsl.h for the
    // dispatch and Recipe-Spatial-Contract-Two-Pass-Culling-Direction-2026-07.md for the
    // design this derisks.
    DeclarePosition        = 112,

    // HAND-MIRRORED ADDITION (Recipe-Nested-Invocation-Unroll-AB-Direction-2026-07 M1):
    // InvokeRecipe is NOT a Yeroket canonical opcode at all (no C# SDFInstruction.cs
    // counterpart exists or is planned — this is a VIXEN-runtime-only bytecode-level
    // concept, same category as DeclarePosition above, not a real [SdfCoreOp]). Value 113
    // chosen as the next free slot after DeclarePosition=112.
    // Semantics: invokes an already-registered recipe (by ID) as a sub-expression of the
    // CALLING recipe's bytecode, so recipes can compose other recipes (not just CSG-combine
    // primitives within one flat bytecode array, which is all today's opcodes support).
    //   - data[0] = calleeRecipeId (encoded as a float, truncated to uint32_t at dispatch —
    //     same immediate-operand convention every other opcode already uses for compile-time
    //     constants; resolved against RecipeRegistry at both interpret-time (SdfRecipeEval.h)
    //     and unroll-time (SdfRecipeCodegenGlsl.h)).
    //   - Position: the callee samples the CALLER's current `pos`/`curPos` unmodified — no
    //     implicit transform is pushed. A caller wanting a translated/rotated nested instance
    //     composes an explicit Transform/RestorePos pair around InvokeRecipe first (the exact
    //     existing precedent, not new semantics) — InvokeRecipe itself is position-passthrough.
    //   - Result composition: pops nothing from the value stack; pushes exactly ONE value (the
    //     callee's evaluated distance), identically to how a leaf primitive (Sphere, etc.)
    //     pushes one value — so existing CSG combinators (Union, SmoothUnion, ...) operate on
    //     a nested-call result with zero changes.
    //   - Cycle/recursion guard: enforced at RecipeRegistry::Register time (walks the callee
    //     graph transitively; see RegisterResult::RecursiveInvocation /
    //     RegisterResult::NestingTooDeep in RecipeRegistry.h) — never a runtime-only check, so
    //     an unguarded cycle can never reach evalRecipe or the GLSL emitter in the first place.
    //   - Max nesting depth: fixed at kMaxRecipeNestingDepth = 4 (RecipeRegistry.h), enforced
    //     by the same registration-time graph walk as the cycle guard.
    // See SdfRecipeEval.h::evalRecipe (interpreter dispatch) and SdfRecipeCodegenGlsl.h
    // (recursive-inlining GLSL dispatch) for the two execution-path implementations, and
    // Recipe-Nested-Invocation-Unroll-AB-Direction-2026-07.md for the full design.
    InvokeRecipe           = 113,
};
}  // namespace Vixen::SVO::Recipe