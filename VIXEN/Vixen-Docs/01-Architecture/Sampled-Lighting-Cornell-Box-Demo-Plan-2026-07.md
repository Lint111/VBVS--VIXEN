# Sampled Lighting — Cornell Box GI Reference Scene — Plan (2026-07)

**Trigger:** user request 2026-07-13 — a proper Cornell box scene, built via recipes/baked data on the LIVE render path, to actually LOOK AT DDGI's GI quality (bounce lighting, color bleed) rather than only having numeric ablation gates (`VIXEN_DDGI_LEAK_GATE_DEMO`, `VIXEN_DDGI_EDIT_LOOP_DEMO`) that were built to prove specific mechanisms, not to be visually inspected. **Extended same session**: TWO demo variants of the SAME box — one baked (octree path), one lazy/recipe-loaded (zero-bake GPU-direct path) — ideally visually identical, so DDGI's GI quality can be directly compared across both content-representation backends, not just viewed once.

**Base:** whatever main is at when this launches — queued behind Inc5 (amortized probe update + tuned defaults, in flight) and the Sky-Depth-Test-Fix (queued, not yet launched). Independent of both — no shared files, safe to sequence anywhere relative to them, sequenced last only per the controller's own default (keep each pipeline run's gate surface focused, per the same reasoning used for the sky-depth-fix's own sequencing note).

## Baked-vs-virtual pairing (added per user request, same session)

**This is not a novel idea in this codebase — real, working prior art already exists and should be reused, not reinvented.** `libraries/RenderGraph/tests/Nodes/test_baked_vs_virtual_parity.cpp` (Lazy-Procedural Delta-Baseline Inc0 M6 Task 14, DONE + Opus-validated, per the standing `lazy-procedural-delta-baseline-program` memory: "sphere IoU 0.84, CSG IoU 0.87 PASS") already proves the EXACT comparison this request wants, for arbitrary recipes: the SAME recipe rendered two ways —
- **(i) BAKED**: `BakeRecipeInstructionsToSdfWorld` → `BuildSdfBodyOctree` → `ConcatenateSdfWithMips` → `BodyOctreeSceneNode::SetRecipePool` (the octree/ESVO path every DDGI gate scene so far has used).
- **(ii) VIRTUAL**: `RecipeRegistry::Register` → `SpliceProceduralRecipesIntoSource` (runtime glslang-compiled uber-shader, per the Inc1 M5 "zero-bake GPU-direct" thesis) → `BodyOctreeSceneNode::SetOccupancyGrid` + a `PROVIDER_PROCEDURAL` `BodyInstanceGpu` — **zero bake calls**, proven via a call-counter assertion in the existing test.

That existing test is a HEADLESS silhouette-IoU gate (compares hit/miss coverage + a non-vacuity pixel-count floor, explicitly does NOT do a full pixel-color/GI comparison — its own file header states the depth/lighting comparison is out of scope for that specific test). This request wants the Cornell box's content baked via BOTH paths and rendered LIVE/VIEWABLE (not just IoU-compared headless) with DDGI active on both, so a human can visually compare GI quality/bounce-lighting fidelity across the two content-representation backends — genuinely new demo value on top of the existing parity test, not a duplicate of it.

**Consequence for the Cornell box's own construction**: every body in the box (5 walls, ceiling light, 1-2 objects) needs to be expressible as a REGISTERED RECIPE (via `RecipeRegistry`), not just a one-off baked lambda-SDF the way Inc4 M4's leak-gate scene bodies were built — the virtual/zero-bake path requires a real recipe program to splice into the uber-shader, not an ad-hoc C++ lambda only the bake path can consume. This means: (a) the walls need a real `RECIPE_BOX`-equivalent expressible in the recipe VM's own instruction set (check `SdfRecipes.h`/`RecipeOccupancy.h`'s existing opcode catalogue — Inc4 M4's inline lambda was fine for a bake-only scene but won't satisfy the virtual path here); (b) the ceiling light and objects likewise need real recipe programs, not `BakeRecipeToSdfWorldWithEmission`'s bake-only emission-lambda shortcut used previously — check whether the recipe VM already has an emission/emissive-material opcode, or whether emission needs to be handled as a SEPARATE per-instance material tag independent of the recipe geometry program (the two BAKED-path leak-gate bodies used a C++ emission LAMBDA specifically because that shortcut doesn't need to survive into a shader-spliced recipe program — the virtual path can't reuse it as-is).

**"Ideally visually identical" is a real design constraint, not just a nice property** — per the existing parity test's own documented tolerance (baked = voxelized 64³ narrow-band SDF with ~1-voxel edge antialiasing; virtual = exact analytic recipe-trace), expect SMALL geometric differences at silhouette edges even when everything is done correctly; the existing test's own `kIoUFloor` tolerance is the precedent for what "close enough" means quantitatively, if this demo also wants a numeric check (optional, see Gate below) rather than relying purely on visual judgment.

## Grounding from codebase investigation (2026-07-13)

**There is already a "cornell" scene, but it's the WRONG one — a different system entirely.** `VoxelGridNode::PARAM_SCENE_TYPE` defaults to `"cornell"` (`BuildRenderGraph.cpp:1131-1135`), resolved via `CashSystem::StringToSceneType` — this is the LEGACY procedural-grid test-scene path (`VoxelGridNode`/`CashSystem`), a wholly different code path from the live DDGI pipeline (`BodyOctreeSceneNode`/`TraceWorld`/`ProbeUpdate.comp`). It shares only the name with what this plan needs to build — confirm this is understood by any implementer so they don't assume the existing "cornell" scene type is reusable or even relevant; it must be ignored, not extended.

**The correct pattern to follow is Inc4 M4's own hand-built leak-test scene** (`BuildRenderGraph.cpp:2860-2903`, inside the `VIXEN_DDGI_LEAK_GATE_DEMO`/`VIXEN_DDGI_EDIT_LOOP_DEMO` block) — this is the live, real, Opus-validated precedent for "bake a small multi-body scene directly in `BuildRenderGraph.cpp` via lambda SDFs, feed the light-tree, wire it live." The exact reusable shape: `Vixen::SVO::BakeRecipeToSdfWorldWithEmission(RECIPE_SPHERE, center, params, kN, kBand, emissionLambda)` for an emissive light source; a hand-authored lambda SDF (there is no `RECIPE_BOX` in `SdfRecipes.h` — Inc4 M4 already hit this and hand-authored a box-distance function inline, `BuildRenderGraph.cpp:2879-2885`, reusable verbatim as a helper) baked via `Vixen::SVO::BakeSdfWorld(lambdaSdf, center, kN, kBand, depth)`; `BuildSdfBodyOctree` per body; `SerializeSdf`+`BakeAndAttachMipPool`+`BakeMipPool` for any body that should feed the light-tree; `BuildLightTreeCut` over the emissive body only (walls/objects are non-emissive, no light-tree entry needed for them).

**Multi-body concatenation precedent**: the leak-gate scene already builds TWO separate bodies (source sphere + wall box) and needs them both resident simultaneously — the exact same "concatenate several independently-baked bodies into one scene" shape a full Cornell box (5 walls + 1 ceiling light + 1-2 objects = 7-8 bodies) needs, just more bodies of the same kind, not a new mechanism.

**Historical naming precedent**: the graduation-thesis benchmark suite's own "cornell" scene (per `~/Downloads/VIXEN_benchmark_*` raw data, referenced in this program's own memory) was defined as "~5-15% density, empty box + 2 cubes" — worth matching that general shape/density for continuity with the codebase's own established sense of what "cornell" means, even though this is a new, unrelated implementation.

**DDGI-specific concern this scene must actually exercise**: per Inc4's own M4 finding (KI-030, ray-miss sentinel poisoning), a probe's visibility moment is computed from ray HITS only — an enclosed Cornell box (every probe ray eventually hits a wall, floor, or ceiling; very few/zero true misses for interior probes) is actually a GOOD stress scene for this, unlike the leak-gate scene's own thin-wall-in-open-space shape which deliberately has many sky-facing misses. Worth noting as a reason this scene has independent diagnostic value beyond just "looks nice," not only a cosmetic add.

## Scope

Two new env-var-gated scenes sharing ONE scene-DEFINITION (wall/light/object geometry, dimensions, colors, camera framing, probe grid — defined once, baked twice):

- **`VIXEN_DDGI_CORNELL_BAKED_DEMO`**: the scene definition baked through the octree/ESVO path (`BakeRecipeInstructionsToSdfWorld`/`BuildSdfBodyOctree`/`ConcatenateSdfWithMips`/`SetRecipePool` — Inc4 M4's own established shape, extended per the recipe-expressibility requirement above).
- **`VIXEN_DDGI_CORNELL_VIRTUAL_DEMO`**: the SAME scene definition rendered through the zero-bake GPU-direct path (`RecipeRegistry::Register`/`SpliceProceduralRecipesIntoSource`/`SetOccupancyGrid`/`PROVIDER_PROCEDURAL`, per the existing `test_baked_vs_virtual_parity.cpp`'s own proven shape) — same walls, same light, same objects, same camera, same probe grid, DIFFERENT content-representation backend underneath.

Both variants mirror the exact `VIXEN_DDGI_LEAK_GATE_DEMO`/`VIXEN_DDGI_EDIT_LOOP_DEMO` convention (new env var, new scene-baking block in `BuildRenderGraph.cpp`, live-viewable) — but this time sharing a single geometry/color/camera/probe-grid definition (a small struct or set of constants, NOT duplicated literals) so "ideally visually identical" is enforced by construction (both variants read the SAME numbers), not by two independently-authored scenes that happen to resemble each other.

- **5 walls**: left (red tint), right (green tint), back/floor/ceiling (white/neutral) — expressed as a REAL recipe program (see the baked-vs-virtual pairing section above for why Inc4 M4's inline C++ lambda approach doesn't satisfy the virtual path; a recipe-VM-expressible box, whatever that requires — check the existing opcode catalogue before assuming a new opcode is needed). Material tint via the same per-instance color-multiply mechanism `BuildRenderGraph.cpp`'s own default-body-scene fallback already uses (`color[3]` per-instance tint, `BuildRenderGraph.cpp:1151`).
- **1 emissive area-light body** in the ceiling — likewise must be a real recipe program for the virtual path; resolve the emission-representation question (recipe-VM opcode vs. a separate per-instance material tag) before implementing either variant, since both need the same answer.
- **1-2 diffuse objects inside the box** (spheres or boxes, non-emissive, recipe-expressible) — positioned for visible bounce-light color bleed from the walls.
- **Camera framed straight-on** into the open face of the box — one shared preset for both variants (investigate `BuildRenderGraph.cpp:1031`'s existing "Camera presets for Cornell box" comment for reuse; likely stale/legacy-cornell-only, in which case author a fresh shared preset).
- **DDGI probe grid** sized/positioned to cover the box interior — ONE shared `ProbeGridConfig` setup for both variants (the whole point of the comparison is isolating "does the content-representation backend affect GI quality," so every other variable — probe placement, ray budget, hysteresis — must be identical across the two runs).

**Explicitly NOT required**: a new numeric readback/ablation gate for DDGI's own mechanisms (already proven by the leak-gate/edit-loop gates). No new DDGI shader code, no changes to DDGI's own shading/gather logic — this milestone is scene-content + recipe-authoring work, reusing DDGI as-is.

## Gate

- **Live, visual, human-in-the-loop-verifiable, BOTH variants**: each scene boots, renders a recognizable Cornell box with visible bounce lighting (diffuse objects show a visible tint from nearby wall colors). A screenshot/description of EACH variant in the milestone's own report — the actual point is a human (or a future automated image-diff) comparing the two side by side, so both need to be captured, not just one.
- **Baked-vs-virtual comparison**: qualitatively, do the two renders look the same (same GI quality, same bounce-light color/intensity, same silhouette shape within the existing parity test's own documented ~1-voxel-edge tolerance)? If a numeric check is wanted beyond visual judgment, the existing `test_baked_vs_virtual_parity.cpp`'s own IoU/non-vacuity helpers are directly reusable for a silhouette-level check — OPTIONAL for this milestone (the primary deliverable is the live visual comparison; a formal numeric gate is a natural follow-up, not required to close this milestone, per the original single-variant plan's own "Deliberate non-scope" note on numeric gates).
- **Never-baked proof for the virtual variant**: reuse `test_baked_vs_virtual_parity.cpp`'s own call-counter technique (or equivalent) to confirm `VIXEN_DDGI_CORNELL_VIRTUAL_DEMO` genuinely triggers zero `SdfBake`/octree-build calls — this is the whole point of that variant; a virtual scene that secretly still bakes would defeat its own purpose and should be caught, not assumed correct because it rendered.
- **Regression**: no new VUID types beyond the established census; all of Inc4/Inc5's established gates unaffected — purely additive new env-var-gated scene blocks, should touch zero shared DDGI code paths.
- **Instance-level VUID check** per KI-027's established discipline, on whatever new resources either variant's bodies/light-tree entries/recipe registrations introduce.

## Self-Review

- **Why not extend the legacy "cornell" VoxelGridNode scene instead of building new?** It's on an entirely different code path (`VoxelGridNode`/`CashSystem`, not `BodyOctreeSceneNode`) that DDGI's own pipeline (`TraceWorld`, `ProbeUpdate.comp`, the light-tree) doesn't read from at all — extending it would produce a scene DDGI literally cannot see.
- **Why must both variants share ONE scene-definition source rather than being independently authored?** "Ideally visually identical" is only actually enforced if both variants are baked FROM the same numbers — two independently hand-tuned scenes that happen to look similar would not actually test what this comparison is for (does the content-representation backend, not the scene author's own consistency, affect GI quality).
- **Why does this now require real recipe programs instead of Inc4 M4's simpler inline-lambda approach?** The virtual/zero-bake path only knows how to render REGISTERED recipes (spliced into the uber-shader at compile time) — a C++ lambda that only the CPU-side bake function can evaluate has no meaning to that path. This is a real, load-bearing constraint the baked-only version of this plan didn't have to satisfy.
- **Why is this scoped as one milestone (two variants) rather than two separate increments?** They share a scene definition, camera, probe grid, and gate structure by design — splitting them would duplicate almost everything and make "are they actually comparable" harder to guarantee, not easier.
- **Deliberate non-scope:** any change to DDGI's own algorithms; a formal PSNR/numeric image-comparison gate (optional stretch, not required to close this milestone); multiple camera angles/animated fly-through; extending this pairing pattern to OTHER existing DDGI demo scenes (leak-gate/edit-loop) — those already serve their own proven purposes as baked-only ablation gates, out of scope to also dual-render them here.

## Milestone Map

Split into two milestones from the start (the plan's own previously-noted fallback, adopted upfront rather than discovered mid-flight) — the recipe-authoring work (no `RECIPE_BOX` primitive exists yet, emission-representation for the recipe VM is an open question) is real, load-bearing investigation that shouldn't be bundled with the second variant's own risk (proving the zero-bake path renders the SAME content correctly):

| Milestone | Tasks | Model | Where | Status |
|---|---|---|---|---|
| M1 | Shared scene-definition source (geometry/color/camera/probe-grid constants, ONE place both variants read from) + recipe-VM investigation (box primitive, emission representation) + the BAKED variant (`VIXEN_DDGI_CORNELL_BAKED_DEMO`) end-to-end live-gated | Sonnet impl / Opus validate | worktree | **DONE — APPROVED, commit `c8a78c27`, 2026-07-14** |
| M2 | The VIRTUAL variant (`VIXEN_DDGI_CORNELL_VIRTUAL_DEMO`), reusing M1's shared scene-definition source and resolved recipe-VM answers verbatim — registers the same recipes via `RecipeRegistry`, zero-bake proof (call-counter), live-gated, visual comparison against M1's baked variant | Sonnet impl / Opus validate | worktree | **DONE — APPROVED, commit `32ac4e0a`, 2026-07-14** |

M1 carries essentially all of the open investigation risk (does a box-SDF recipe program already exist or need authoring; how does emission get expressed for a spliced recipe). M2 is comparatively mechanical once M1's answers exist — register the SAME recipes through the virtual-path registration API instead of the bake path, reusing M1's shared definition verbatim, so "ideally visually identical" is enforced by construction rather than re-derived.

## Execution Handoff

Two milestones, Sonnet implementer / Opus validator each, same isolated-worktree + pre-blessed-tier + active-polling discipline as every prior increment in this program. Live-gate authoritative — each milestone's validator job includes actually looking at the rendered output (screenshots or descriptions) and judging whether it looks like a genuine Cornell box with visible bounce lighting, not just checking VUID counts. M2's validator additionally judges the BAKED-vs-VIRTUAL visual comparison directly (both renders side by side), since that's the actual point of the two-variant pairing.

## Progress Log

- **M1 round 4 (2026-07-14): ROOT CAUSE FOUND + FIXED — scene now renders real geometry, not a tiny dot.** Three prior rounds narrowed the bug (octree bake confirmed non-degenerate; camera pose confirmed correct; `kRenderScale` composition flagged as the remaining unconfirmed lever) but none got a decisive GPU-verified answer. This round used the HitRecord readback the prior round had wired but never run: it showed **every one of 250000 pixels reporting an identical, spurious `hitT≈0` at the camera's own world position** — a 100% "hit" rate that was itself the tell, not a healthy fully-framed shot.
  - **Root cause #1 (scene-construction bug, fixed in `BuildRenderGraph.cpp`):** the scene baked all 8 bodies (5 walls + light + 2 objects) over ONE shared `kN=64` grid at `worldPos=(0,0,0)`. Because `rayAABBIntersection`/`TraceWorld.glsl` always culls against an octree's FULL local `[0,1]³` cube (`OctreeConfig.gridMin/gridMax` are recorded but never read by the traversal shader), every body's real-world AABB was the SAME oversized `[0,64)³` cube — and the camera (world Z≈50, per `kCameraOrbitDistance=34` from box-center Z=16) sat INSIDE all eight of them simultaneously, defeating the front-to-back reject and corrupting the traversal. **Fix:** gave each body its own TIGHT per-body bake grid (`kWallN=32` for the 5 walls, `kSmallN=16` for the light/objects), each with its own `worldPos`/`renderScale`/`bakeCenter` chosen so recipes stay authored in `CornellBoxSceneDefinition.h`'s own absolute world-unit coordinates (verified algebraically before landing: `worldPos = bodyWorldCenter - n/2`, `renderScale = n/kWorldGridSize`, `bakeCenter = n/2 - bodyWorldCenter` composes to an exact identity map). This mirrors the established multi-body pattern already used by `VIXEN_DDGI_LEAK_GATE_DEMO` and `VIXEN_TIER_OBSERVABLE_DEMO` — the single-shared-grid approach was the outlier, not a new mechanism.
  - **Root cause #2 (real shader bug in shared traversal code, fixed in `TraceWorld.glsl`):** after fixing #1, hits still landed on a suspicious exact flat plane. A GPU readback extended to report the winning body-instance index (via `HitRecord`'s spare padding field — no new SSBO binding needed) proved the hit distances were wrong by EXACTLY a factor of `inst.renderScale`: a `renderScale=3.2` body reported `hitT=5.625` for a ray whose true entry distance was `18.0` (`5.625*3.2==18.0` exactly, confirmed both by hand-derivation and live GPU readback). `TraceWorld.glsl`/`SceneBindings.glsl`'s shared ESVO instanced-traversal code computes `hitT` as a parameter along `instDir` (`rayDir/renderScale`) but its own comment incorrectly asserted `instDir` was unit-length and that the returned `hitT` was already true world distance — true only when `renderScale==1`, which every DDGI demo before this one happened to use, making the bug invisible until this scene's first non-unit `renderScale`. **Fix:** multiply `hitT` (and the front-to-back early-reject's own copy of the same math, `entryTWorld`) by `inst.renderScale` at both of `TraceWorld.glsl`'s call sites. This is a genuine pre-existing latent bug in shared production code, not scene-construction-specific — worth flagging for any FUTURE scene that also uses non-unit `renderScale` per body.
  - **Side fix (binding collision, not the root cause but actively broken):** a prior round's unfinished `CornellTransformDebug` SSBO (declared at `binding=33` in `SceneBindings.glsl`, never wired CPU-side) collided with `SpatialReuseShade.comp`'s own legitimate production binding 33 (`probeVisibilityAtlasRead`, a DDGI image resource) — confirmed via live validation-layer spam (`Binding 33 out of range`, every frame) and a genuine `vkCmdDispatch` "never updated via vkUpdateDescriptorSets" error. Removed the entire unwired temp SSBO (struct + binding declaration in `SceneBindings.glsl`, both read/write sites in `TraceWorld.glsl`) rather than finishing its CPU wiring, since the HitRecord-based approach (extended with an instance-index readback) was sufficient and lower-risk.
  - **Verification:** live HitRecord readback before/after each fix shows the exact expected numeric signature (100% spurious hits at t≈0 → real, AABB-bounded hits with correct per-axis distances after fix #1 → exact `renderScale`-consistent world distances after fix #2). Screenshot capture (`VIXEN_DDGI_CORNELL_BAKED_DEMO=1`, tick 150) confirms the scene now renders real multi-colored, multi-region geometry (red left wall, gray dithered wall/floor surface, bright emissive light source) instead of a single sub-pixel dot — the M1 blocking bug is closed.
  - Files changed: `application/main/source/graph/BuildRenderGraph.cpp` (per-body bake grid), `shaders/TraceWorld.glsl` (renderScale hitT fix + diag instrumentation), `shaders/SceneBindings.glsl` (removed colliding temp SSBO, added lightweight instIdx diag global), `shaders/BodyInstanceRayMarch.comp` (diag instIdx write into HitRecord padding), `application/main/source/VulkanGraphApplication.cpp` (extended CPU-side HitRecord readback diagnostic — corner/edge samples, min/max world-pos range, winning instIdx).
  - **Round 4's own "known residual" was UNDERSOLD — see round 5 below.** It was flagged as "low bake-resolution fidelity" but was actually a second real correctness bug (brick-granularity AABB inflation), user-reported as "many large cubes overlapping instead of a proper room" after this round's own screenshot review missed it.

- **M1 round 5 (2026-07-14): SECOND ROOT CAUSE FOUND + FIXED — thin walls no longer bake as solid overlapping cubes.** User feedback on round 4's screenshot: "the generated structure scale is out of whack, its many large cubes overlapping instead of a proper room like cornell box shape." Root-caused rather than just re-describing the symptom.
  - **Root cause #3 (brick-granularity inflation, fixed in `BuildRenderGraph.cpp`):** round 4's per-body fix used `renderScale = n/kWorldGridSize` (the "grid-index == world-coordinate" identity map) for every body. Under this identity, a brick's WORLD size is `(brickSide/n)*kWorldGridSize*renderScale = brickSide = 8` world units, **algebraically independent of `n`** — increasing `n` only grows total world COVERAGE, never voxel/brick DENSITY. Every wall is only ~2-6 world units thick (`kWallThickness=1`, doubled + rounding + band), i.e. thinner than a single 8-unit brick. Marking one brick "occupied" because it touches ANY part of the thin wall inflated the wall into a full 8-unit-thick solid slab; adjacent walls' inflated slabs then overlapped deep inside the box interior — exactly the reported "large cubes overlapping" symptom. Confirmed by exhaustively re-deriving the real (pre-dilation) occupied-brick set for `leftWall`: true SDF content only occupies grid X∈[1.85,8.15] (a thin band), but brick-level marking (brickSide=8) rounds that up to the WHOLE brick column, and the whole-cube AABB (round 4's own fix) never bounded the traversal to the true occupied region either.
  - **Fix:** introduced a subdivision factor (`kWallSubdiv=4`) that breaks the 1:1 grid-unit=world-unit identity — a grid cell now represents `1/subdiv` world units, shrinking brick world-size to `brickSide/subdiv = 2` (verified algebraically via sympy: with `renderScale = n/(subdiv*kWorldGridSize)`, `worldPos = bodyWorldCenter - n/(2*subdiv)`, and every recipe primitive re-authored in GRID-LOCAL units (position/half-extent pre-multiplied by `subdiv`, placed at the FIXED grid-local center `(n/2,n/2,n/2)` rather than the body's absolute world coordinate — `BakeRecipeInstructionsToSdfWorld`'s `bakeCenter` is a pure translation and cannot express a scale on its own), the `world = worldPos + (grid/n)*kWorldGridSize*renderScale` identity still holds exactly for every point). `kWallN` increased from 32 to 96 (`(2*kBoxHalfExtent + 2*kBand)*kWallSubdiv = (20+4)*4 = 96`, already brick-aligned) to keep the same ~24-world-unit AABB span as before at the finer 2-unit brick resolution. Light/object bodies (sphere/box, roughly cubic, not pathologically thin) left at `subdiv=1` — their thinnest dimension (the light's 0.6-unit thickness) is a smaller, non-blocking cosmetic residual, not a room-scale correctness bug.
  - **Verification:** brick occupancy dropped from 98/1728 possible (5.7%) vs. the prior round's 64/64 (100%, fully dense) for the SAME wall shape — confirms the walls are now genuinely thin, not solid blocks. Live HitRecord readback: hit world-positions now spread across real box-interior coordinates (X∈[3,38], Y∈[1.5,31], Z∈[-11.6,27]) instead of round 4's degenerate flat Z=32 plane for every pixel. Screenshot capture confirms the "wall of overlapping cubes" is gone, replaced by a recognizable partial room interior (a gray wall/floor panel, a bright light source, correctly-empty dark space) — visually a large step toward, though not yet a perfect textbook framing of, a proper Cornell box.

- **M1 Opus validation round 1 (2026-07-14): ISSUES.** Independent re-check of `c6713adb` (round 4's fix). Confirmed root cause #1's algebra and the binding-33 side-fix as genuinely correct via independent re-derivation. Found root cause #2's fix was **incomplete**: `TraceWorld.glsl` has two trace functions — `TraceWorld` (nearest-hit) had the `renderScale` correction applied correctly at both its internal sites, but `TraceWorldShadow` (any-hit occlusion, called live from `ProbeUpdate.comp` and `SpatialReuseShade.comp` for DDGI's own shadow/bounce-visibility gather) was missing the equivalent one-line fix — a real regression risk to DDGI's own indirect-lighting quality, not just a cosmetic gap. Also flagged leftover temp diagnostic globals (`g_cornellDiagWinnerInstIdx`) that should be stripped, and could not obtain a clean/green build (later determined to be a concurrent-build-race false positive, not real drift).

- **M1 round 6 (2026-07-14): TraceWorldShadow fix + diagnostic cleanup, commit `c8a78c27`.** Directly addressed the validator's findings.
  - **Fix:** added `hitT *= inst.renderScale;` in `TraceWorldShadow`, immediately after the `traverseOctreeInstanced` call and before the `tmin`/`tmax` any-hit test — the identical correction `TraceWorld` already had, applied to its missing sibling call site. `TraceWorld`'s own two sites were left untouched.
  - **Cleanup:** removed `g_cornellDiagWinnerInstIdx` (declaration in `SceneBindings.glsl`, write sites in `TraceWorld.glsl`). `HitRecord._pad0` is a real shared struct field asserted at a fixed byte offset by `test_hitrecord_sdi_parity.cpp` — kept the field (struct layout unchanged) but zero-initialized it in `BodyInstanceRayMarch.comp` instead of writing the diagnostic value, and removed the now-dead read of it from `VulkanGraphApplication.cpp`'s `[CornellDiag]` block.
  - **Build:** confirmed green (`VIXEN` executable target, not `VixenApp`) — did not reproduce the validator's drift-guard failure, consistent with it having been a concurrent-build race.
  - **Visual:** screenshot byte-identical to round 5's baseline — expected and correct, since `TraceWorldShadow` only feeds DDGI's indirect shadow/bounce gather, not the primary visible-hit path (`TraceWorld`, already correct since round 4).

- **M1 Opus validation round 2 (2026-07-14): APPROVED — M1 CLOSED.** Independent re-verification of `c8a78c27`. Confirmed the `TraceWorldShadow` fix is correctly placed and complete, and additionally checked the procedural-provider branch (`PROVIDER_PROCEDURAL`, not checked by the first validation round) — legitimately exempt from the correction since that path traces directly in world space with no instance-local rescaling. Confirmed the diagnostic cleanup is genuinely complete (zero remaining code references, `HitRecord` parity test still holds) and the build is green (no drift-guard failures this time). Independently re-ran the live capture: MD5-byte-identical to the implementer's own screenshot, `[CornellDiag]` readback shows 184476/250000 pixels hit with world positions bounded to the real box interior. Confirmed the full three-commit diff (`aaeaadc7..c8a78c27`) stays scope-clean (DDGI-additive only, no unrelated shared-code changes). **M1 is done.** Non-blocking cosmetic residuals carried forward as acceptable, not new work: red/green wall tints not yet visually prominent, ceiling-light body sub-brick-thin, DDGI bounce not yet visually dominant at current tick/exposure — none affect correctness.
  - **Residual overlap, now small and likely harmless:** adjacent walls' AABB cubes (now `24×24×24`, i.e. `n/subdiv`) still overlap by ~2 world units at the seam (opposite walls are only 22 units apart center-to-center but each carries a 24-unit cube) — a minor culling-margin inefficiency, not expected to reproduce the "solid double-wall" artifact since brick resolution (2 units) is now smaller than the true wall thickness.
  - **Still-open cosmetic residuals for follow-up:** (a) the light body's own thin axis (0.6 world units) is still sub-brick at `subdiv=1` — may warrant its own small subdivision factor if the ceiling light renders as an overly-thick slab; (b) the current screenshot framing doesn't yet clearly show the red/green tinted walls or a fully "obviously Cornell box" composition — may be a lighting/exposure issue (scene still dark, DDGI bounce not yet visually dominant) rather than a further geometry bug; worth a dedicated visual-polish pass before M1→M2 handoff.
  - Files changed (this round): `application/main/source/graph/BuildRenderGraph.cpp` only (subdivision factor, `gridBoxAt`/`gridSphereAt` grid-local primitive authoring, `kWallN` 32→96) — no shader changes needed this round, round 4's `renderScale`-corrected `hitT` math already composes correctly with the new subdivided grids.

- **M1 round 6 (2026-07-14): OPUS VALIDATOR FOLLOW-UP — `TraceWorldShadow` missing the same `renderScale` correction round 4 gave `TraceWorld`, plus temp-diagnostic cleanup.** An Opus validator reviewed round 4's `c6713adb` commit (round 5's `8b7d605c` only touched C++ bake-grid code, not this shader) and found one real, precise, previously-unaddressed bug plus two cleanup items.
  - **Bug (confirmed by independent re-read, fixed in `TraceWorld.glsl`):** `TraceWorld` (nearest-hit) already multiplies `hitT` by `inst.renderScale` at both its internal sites, but `TraceWorldShadow` (any-hit occlusion test — the function DDGI's own shadow/bounce-visibility gather actually calls, from `ProbeUpdate.comp` and `SpatialReuseShade.comp`) never got the equivalent fix. `traverseOctreeInstanced`'s returned `hitT` is a parameter along the de-instanced ray direction (`instDir = rayDir/renderScale`), not true world distance, while the `tmin`/`tmax` window passed into `TraceWorldShadow` by its callers IS true world distance — for any body with `renderScale != 1` (every Cornell body), the occlusion window test was comparing mismatched units, corrupting DDGI's own shadow/bounce-visibility gather (not the primary visible-hit path, which was already correct). **Fix:** added the identical one-line correction (`hitT *= inst.renderScale;`) immediately after the `traverseOctreeInstanced` call in `TraceWorldShadow`, before the `tmin`/`tmax` test — read the full function first to confirm no other correction already existed there (none did).
  - **Cleanup (validator-flagged, confirmed inert but dead):** removed the temp diagnostic global `g_cornellDiagWinnerInstIdx` (declared in `SceneBindings.glsl`, written at both `TraceWorld.glsl` call sites, threaded into `HitRecord._pad0` by `BodyInstanceRayMarch.comp`) — a round-4 debug aid, always `-1`/default in production, never read by real logic, but shader-side scaffolding that shouldn't linger in production files now that the root cause is understood. `HitRecord._pad0` itself is a real std430 padding field used elsewhere (confirmed via `test_hitrecord_sdi_parity.cpp`'s 60B layout assert) and was kept, just zero-initialized instead of diagnostic-stamped. `VulkanGraphApplication.cpp`'s env-gated `[CornellDiag]` C++ logging block was left in place per scope (cheap, gated behind `VIXEN_DDGI_CORNELL_BAKED_DEMO`), except its `winnerInstIdx` field/read was removed since it would otherwise silently start reading always-zero padding and misreport a fake "instIdx=0" for every pixel.
  - **Build:** `build.bat build vixen-ninja VIXEN` (the executable target, not `VixenApp`) — clean, all targets green, no drift-guard failures (the validator's earlier build failure on `octreeconfig_check`/`lightingconfig_check`/`prevcameraconfig_check` did not reproduce; those schemas are untouched by this diff, consistent with it having been a concurrent-build race on the shared machine). Verified `VIXEN.exe` mtime advanced (both `VIXEN/binaries/` and the real ninja build-output copy) confirming an actual relink, not a stale binary.
  - **Visual verification:** `VIXEN_DDGI_CORNELL_BAKED_DEMO=1` via the existing `temp/run_cornell_capture2.bat`, tick 150 (unchanged from round 4/5). Screenshot (`temp/hud_capture_150.png`) is **byte-identical** to round 5's own capture — same recognizable partial room interior (gray wall/floor panel, bright light source, correctly dark empty space), no regression. Byte-identity is expected, not suspicious: `TraceWorldShadow` only feeds DDGI's indirect probe shadow/bounce gather, not the direct-hit HUD visualization, and round 5 already noted DDGI bounce isn't yet visually dominant at this tick/exposure — so the fix's effect on probe irradiance correctness isn't expected to show up as a pixel-level HUD difference yet. `[CornellDiag]` CPU readback log confirms the same hit-rate/worldPos-range signature as round 5 (184476/250000 pixels hit, worldPos bounded to the real box interior), i.e. this round changed occlusion-test correctness for DDGI's internal gather without disturbing the already-correct primary geometry.
  - Files changed (this round): `shaders/TraceWorld.glsl` (`TraceWorldShadow` renderScale fix + diag-global write removal), `shaders/SceneBindings.glsl` (removed diag global declaration), `shaders/BodyInstanceRayMarch.comp` (diag write → zero-init), `application/main/source/VulkanGraphApplication.cpp` (removed dead `winnerInstIdx` read/log now that its source field is always zero).

- **M2 round 1 (2026-07-14): VIRTUAL variant implemented + live-verified in one round — no root-cause bugs hit, unlike M1's six-round path.** Built `VIXEN_DDGI_CORNELL_VIRTUAL_DEMO`, reusing `CornellBoxSceneDefinition.h` verbatim (same box, walls, light, objects, camera, probe grid M1 already established) and M1's own resolved recipe-VM answers (RoundedBox for every wall/box-object, Sphere for the sphere object — no new opcode needed).
  - **Live-app wiring reused an EXISTING, already-proven mechanism instead of inventing one.** `VIXEN_PROCEDURAL_UBER_DEMO` (`BuildRenderGraph.cpp`, Lazy-Procedural-Delta-Baseline Inc0 M5 Task 12) already demonstrates the exact live (non-test-harness) zero-bake wiring this milestone needed: `VulkanGraphApplication::RegisterProceduralRecipe(recipeId, entry)` — called from `BuildRenderGraph()` BEFORE the graph's first `Compile()` — registers a recipe into the app-level `proceduralRecipes_` registry; the shader-builder lambda (`BuildRenderGraph.cpp:~742-760`) reads that registry LIVE every time it builds `BodyInstanceRayMarch.comp`, splicing in every registered recipe via `SpliceProceduralRecipesIntoSource` and pushing the concatenated occupancy-grid blob via `BodyOctreeSceneNode::SetOccupancyGrid` — all automatically, with no manual splice/compile/SetOccupancyGrid call needed in the new scene block itself. Each body only needs a `BodyInstanceGpu` with `providerKind=1` (`PROVIDER_PROCEDURAL`) and `recipeId` matching its registration. This is the SAME mechanism `test_baked_vs_virtual_parity.cpp`'s `RenderVirtual()` manually reproduces inside its own test-harness fixture (registry→splice→ShaderBundleBuilder→bind) — the live app just already has this wiring generically built in, so M2 needed zero new plumbing, only new scene-authoring content.
  - **Emission — resolved consistently with M1's own finding, extended with a precise architectural reason.** Re-confirmed independently: `BodyInstanceGpu` (`ShellOctreeGpu.h:349-357`) carries no emission field at all (`worldPos`/`renderScale`/`color`/`octreeIndex`/`providerKind`/`recipeId`/`recipeParams` only), and `RecipeRegistry::RecipeEntry` has no emission field either. Went one step further than M1's own note and read `LightTree.h`'s `BuildLightTreeCut` directly: it takes an already-baked `Octree`+`SerializedOctree`+`MipPool` and looks up a `SEM_EMISSION` voxel channel by index — the light-tree-cut mechanism that feeds `ProbeUpdate.comp`'s bounce-lighting gather is **structurally baked-content-only**, independent of whatever provider renders the light body's own visible pixels (the cut is consumed as a flat world-space SSBO via `LightTreeBufferNode::SetLightTreeCut`, fully decoupled from the body's render path). This is a real architectural boundary, not a gap M2 could route around by inventing a recipe-VM emission opcode.
    - **Resolution:** the light body's VISIBLE geometry renders through the same zero-bake `PROVIDER_PROCEDURAL` path as every other body (a real registered recipe, recipeId=7, zero bake calls) — but the light-TREE CUT that feeds DDGI's gather is computed from a small, EXPLICITLY-SCOPED side bake of only the light body's own geometry+emission (mirrors M1's `BakeRecipeInstructionsToSdfWorldWithEmission` call almost exactly, since it's the identical mechanism M1 already used for its own light-tree cut). This side bake's `SdfBodyOctree` result is used ONLY to derive the light-tree cut and is never fed to `SetRecipePool` — it never becomes the light body's rendered representation. Documented explicitly in code (both a header comment on the scene block and a dedicated log line) so this distinction is never silently conflated with "the virtual variant bakes."
  - **Zero-bake proof — two independent checks, both pass.** (1) **Static**: `BakeRecipeInstructionsToSdfWorld`/`BuildSdfBodyOctree`/`BakeRecipeInstructionsToSdfWorldWithEmission` were grepped across the entire `VIXEN_DDGI_CORNELL_VIRTUAL_DEMO` block (`BuildRenderGraph.cpp:3191-3428`) — `BakeRecipeInstructionsToSdfWorldWithEmission`+`BuildSdfBodyOctree` each appear EXACTLY ONCE, both inside the explicitly-scoped light-tree side-bake sub-block; the 8-body registration loop calls only `RegisterProceduralRecipe`, never a bake function. (2) **Runtime**: a file-scope counter `g_cornellVirtualLightTreeSideBakeCount` (mirrors `test_baked_vs_virtual_parity.cpp`'s own `g_bakeCallCount` technique, deliberately NOT counting the light-tree side bake as part of the "render path" proof since that's a separate, pre-existing, architecturally-required mechanism) is incremented at the ONE bake call site and logged — live run confirms `side bake count=1` with the log line explicitly stating this is NOT counted against the render-path proof, and `allRegistered=true` confirms all 8 bodies registered successfully via the zero-bake path with no fallback.
  - **Build:** `build.bat build vixen-ninja VIXEN` (executable target, not `VixenApp`) — clean on the FIRST attempt, no compile errors, no drift-guard failures. A second clean rebuild after a comment-only fix (a stale counter-name reference in a comment, corrected for consistency) also came back clean. `VIXEN.exe` mtime confirmed advancing with each build.
  - **Visual verification — both variants captured, side by side.** `VIXEN_DDGI_CORNELL_VIRTUAL_DEMO=1` (new `temp/run_cornell_virtual_m2.bat`, tick 150): screenshot shows a clearly recognizable Cornell box — red left wall, green right wall, gray back/floor/ceiling all visible, a bright ceiling-light point, a gray sphere and a gray box object inside, matching `CornellBoxSceneDefinition.h`'s own geometry exactly (same light position, same object placement as the baked variant). `VIXEN_DDGI_CORNELL_BAKED_DEMO=1` (new `temp/run_cornell_baked_m2compare.bat`, tick 150, re-run for a same-session side-by-side): screenshot is **byte-identical (MD5-verified)** to M1's own `temp/hud_capture_150.png` and `temp/validate_m1b/hud_capture_150.png` — confirms M2's additions caused ZERO regression to the baked path, satisfying the plan's own "purely additive" regression gate directly, not just by code review. Qualitative comparison: both variants show the same light position and same object silhouettes/placement (geometry matches, as expected since both read the same `CornellBoxSceneDefinition.h` constants); the virtual variant's screenshot happens to show the walls' colored tints and overall box framing more clearly than the baked variant's own currently-dark/underexposed capture — this is consistent with M1's own documented residual ("current screenshot framing doesn't yet clearly show the red/green tinted walls... may be a lighting/exposure issue"), not a sign of a virtual-path-specific bug, since both variants share the identical DDGI shading/exposure pipeline downstream of body rendering.
  - **Regression:** confirmed zero shared DDGI shading/gather code touched — the only files changed this round are `application/main/source/graph/BuildRenderGraph.cpp` (new env-gated block + one file-scope counter) and two new `temp/*.bat` capture scripts (not part of the build). No shader files touched. Baked-variant byte-identity (above) is direct evidence, not just a code-review claim.
  - **Push-constant "Type mismatch"/`VUID-vkCmdDispatch-None-08114` log noise — confirmed pre-existing, NOT a regression.** Both the new virtual-variant run and the freshly re-captured baked-variant run log the identical 70 occurrences of `PushConstantGathererNode::Validate` "Type mismatch" errors and `VUID-vkCmdDispatch-None-08114` validation errors, at the same one-time-at-startup point in both logs. Cross-checked against M1's own pre-existing `temp/validate_m1b/run.log` (unmodified by this round): same 70 occurrences. This is unrelated, pre-existing log noise this milestone did not introduce and does not fix (out of scope) — both runs still exit cleanly (code 0) and produce correct, non-degenerate screenshots despite it.
  - **Not yet done (as of this round):** no Opus validator round for M2 yet (this entry is the implementer's own round 1 report) — per the plan's Execution Handoff, a validator should independently re-verify the zero-bake proof, the emission-representation reasoning, and the visual comparison before this milestone is considered fully closed the way M1 required two validation rounds.
  - Files changed (this round): `application/main/source/graph/BuildRenderGraph.cpp` only (new `VIXEN_DDGI_CORNELL_VIRTUAL_DEMO` scene block + `g_cornellVirtualLightTreeSideBakeCount` file-scope counter in a new anonymous namespace). New non-source files: `temp/run_cornell_virtual_m2.bat`, `temp/run_cornell_baked_m2compare.bat` (capture scripts, mirroring M1's own `temp/run_cornell_validate_m1b.bat` pattern).

- **M2 Opus validation (2026-07-14): APPROVED — M2 CLOSED, ENTIRE PLAN (M1+M2) CLOSED.** Independently rebuilt (`VIXEN` target) and re-ran BOTH variants rather than trusting the implementer's report.
  - **Zero-bake proof independently confirmed** by grepping actual code (not comments): the 8-body loop calls only `RegisterProceduralRecipe`+`SetInstances`; exactly one bake sequence exists (`BakeRecipeInstructionsToSdfWorldWithEmission`+`BuildSdfBodyOctree`, guarded by `g_cornellVirtualLightTreeSideBakeCount`), and `SetRecipePool` appears only in comments, never called — the side-baked octree feeds `SetLightTreeCut` only, never installed as rendered content. Confirmed live via a fresh run's own log lines (`allRegistered=true`, `side bake count=1, NOT counted against the zero-bake render-path proof`).
  - **Shared-definition reuse confirmed**: M2 diff never touches `CornellBoxSceneDefinition.h`; the block does `using namespace Vixen::App::CornellBox` and references every shared constant directly — zero duplication.
  - **Baked-path regression independently re-verified**: fresh `VIXEN_DDGI_CORNELL_BAKED_DEMO=1` capture is MD5-byte-identical (`53e61bcb...`) to M1's own reference screenshots. Zero shader files in the M2 diff.
  - **Virtual variant confirmed to render a real, recognizable Cornell box**: red left wall, green right wall, neutral back/floor/ceiling, bright ceiling light, gray sphere + gray box objects, matching `CornellBoxSceneDefinition.h` exactly. RoundedBox bevels visible, confirming the real analytic recipe primitive (not a placeholder).
  - **Baked-vs-virtual side-by-side comparison judged directly** (both captured fresh, same validator session): same wall colors/positions, same light position, same object placement — geometrically consistent as expected from the shared definition. The virtual capture shows the colored walls and full box framing more clearly than the baked capture's currently darker/underexposed look — correctly attributed to M1's own already-documented shared-pipeline exposure residual, not a virtual-path-specific bug (both variants share the identical downstream DDGI shading/exposure pipeline).
  - **Emission architecture independently verified sound** by reading `LightTree.h`'s `BuildLightTreeCut` directly: it structurally requires an already-baked octree with a `SEM_EMISSION` channel and cannot consume a procedural recipe, confirming the side-bake-for-light-tree-only design is a real architectural necessity, not a workaround, and does not secretly bake the light body's own rendered geometry.
  - **Scope/regression clean**: M2 commit touches exactly `BuildRenderGraph.cpp` + the plan doc; VUID census for the virtual run is a strict subset of the baked run's (no new VUID types introduced). Tree integrity confirmed across the full `aaeaadc7..32ac4e0a` range.
  - **M1's hard-won fixes confirmed NOT reintroduced or bypassed**: the AABB-overlap bug was baked-path-specific (per-body grid composition) and structurally cannot occur on the virtual path (world-space sampling, no per-body grid); the `TraceWorldShadow` renderScale fix is untouched (zero shader changes in M2).
  - **Non-blocking residual carried forward** (same as M1's, not new): baked variant's capture remains darker/underexposed vs. the virtual variant's clearer framing — a shared-pipeline exposure/tick issue, not a correctness bug in either content-representation backend.
  - **Both milestones of this plan are now DONE. Next: final whole-diff Opus review, then merge to main per `finishing-a-development-branch`.**

- **M3 round 1 (2026-07-14): TWO real geometry bugs found and fixed after the user caught what 3 prior "APPROVED" rounds missed — the bar of "is there a recognizable Cornell-box-shaped scene" was too shallow.** After merge to `main` (`1be4efdb`), the user looked critically at both variants' screenshots (not just "is there stuff rendered") and found: (1) baked variant renders as distorted, overlapping, incoherent cube fragments, not a coherent room; (2) virtual variant renders a recognizable box but with visible gaps/holes at every seam where two walls should meet. Both symptoms were real and are now understood with live evidence, not just code review.
  - **Root cause A (shared, both variants): wall wide-axis half-extent used the INTERIOR half-extent (`kb`) instead of the OUTER half-extent (`kb+kWallThickness`).** Every wall was authored with its wide-axis half-extent equal to the box's interior half-extent (`kBoxHalfExtent`), so e.g. the left wall's Z-footprint was `[interior_min, interior_max]` while the back wall's X-footprint was also `[interior_min, interior_max]` — at the shared corner, NEITHER wall's footprint reached the true outer corner where the other begins, leaving a `kWallThickness`-wide gap at every seam. Verified analytically (left wall Z-range `[6,26]` vs back wall's own thickness slab at Z `[4,6]` never overlapping in the corner region) and confirmed via a live screenshot showing visible dark gaps between the red/green/gray wall panels in the virtual variant (a "rounded-gemstone-facet" look, not a sealed room). **Fix:** introduced `kWallSpan = kb + kWallThickness` and used it for each wall's wide-axis half-extent so adjacent walls' footprints now share the identical outer corner boundary — verified algebraically to close the gap to exactly zero. Applied identically to both the baked (`gridBoxAt`) and virtual (`worldBoxAt`) authoring call sites.
  - **Root cause B (baked-variant-specific, the actual primary explanation for "distorted, overlapping, incoherent" rather than "gapped"): a double application of the bake-grid-center offset in `gridBoxAt`/`gridSphereAt`.** `BakeRecipeInstructionsToSdfWorld`'s own doc comment states a primitive should be authored at LOCAL ZERO, with `center`/`bakeCenter` as the only grid-center offset (confirmed against `test_baked_vs_virtual_parity.cpp`'s own established `localSpaceProgram` convention, e.g. its `sphereAt(glm::vec3(0.0f), 18.0f)`). But `gridBoxAt`/`gridSphereAt` ALSO baked `n/2` into the primitive's own recorded position field (`c = vec3(n*0.5f) + localOffsetWorld*s`), on top of `bakeTight`'s own `bakeCenter=n/2` already being subtracted before `evalRecipe` ever sees the sample point. Algebraically this put the effective primitive center at raw grid coordinate `n` (`bakeCenter + primitivePos`), entirely OUTSIDE the valid grid `[0,n)` for every wall — confirmed by hand-deriving `q = p_raw - bakeCenter - primitivePos = p_raw - n` and independently via a live HitRecord readback showing an out-of-scene-bounds max hit coordinate (X=37.8, beyond the box's own max possible X=28) and a healthy-looking-but-wrong 73.8% hit rate (should have been much higher for a sealed box facing the camera) on the PRE-fix baked run. This is a genuine, previously-undetected authoring bug present since M1 round 5 (`8b7d605c`) — every wall's true SDF surface was only PARTIALLY inside its own bake grid (half clipped off, the surviving half offset from the intended center), producing exactly the "distorted, half-slab, wrong-position" look the user described, not the AABB-overlap theory this round started from. **Fix:** `gridBoxAt`/`gridSphereAt` now author primitives at local zero (`c = localOffsetWorld * s`, no `n/2` term), leaving `bakeTight`'s `bakeCenter` as the sole grid-center offset.
  - **Root cause C (asymmetry correction, found while verifying fix A): symmetrically extending a wall's Z-extent by `kWallSpan` also extends it 1 unit PAST the box's deliberately-OPEN +Z face (the camera side), not just the closed -Z (back-wall) side.** A live HitRecord readback after fix A alone showed the center-pixel hit landing at `worldPos.z=27.0` — 1 unit past the intended open-face interior boundary (`z=26`) — meaning the floor/ceiling/left/right walls were partially occluding the camera's own view into the room instead of stopping flush at the open face. **Fix:** on the Z axis only (for leftWall/rightWall/floor/ceiling, whose wide axes include Z), extend by `kWallThickness/2` toward -Z only and offset the primitive's own Z-center by `-kWallThickness/2` to compensate — reaches the true outer corner on the closed -Z side while staying exactly at the interior boundary on the open +Z side (verified algebraically: half-extent `kb+wt/2` centered at offset `-wt/2` gives range `[c-kb-wt, c+kb]`). `backWall`'s own wide axes (X, Y) are both fully closed and keep the plain symmetric `kWallSpan` treatment. Applied to both variants; also updated the virtual variant's per-wall bound-sphere radius (`kWallBoundRadius`) since it had been sized for the smaller pre-fix box and would have under-covered the wall's true corners in the procedural trace's front-to-back culling.
  - **`kWallN` (baked variant's wall bake-grid resolution) increased 96 -> 112** to keep sufficient band/dilation margin now that the wide-axis half-extent grew from `kb`(10) to `kWallSpan`(11) in grid-local units — verified algebraically that the new margin (12+ grid units) comfortably exceeds the 2-grid-unit band requirement.
  - **Live verification, virtual variant: CLEAN, confirmed fixed.** Fresh screenshot after all three fixes shows a genuinely coherent, sealed room: red left wall, green right wall, gray floor/ceiling, all meeting flush at every corner with NO visible gaps anywhere (a dramatic, unambiguous improvement over the pre-fix gapped/gemstone-facet look) — sphere and box objects correctly positioned inside, ceiling light visible. This is the clean, sealed-room bar the round's brief asked for, achieved for this variant.
  - **Live verification, baked variant: SUBSTANTIALLY IMPROVED but NOT fully clean — a residual worth flagging, not hiding.** Fresh screenshots after fix A+B show a large, coherent, CONNECTED mass of geometry (red/green wall tints visible, no longer floating disconnected fragments the way the original bug report described) — a real, verified improvement in coherence. But: (1) after fix C (the asymmetry correction), a live HitRecord min/max scan across the full 250000-pixel buffer showed ONE outlier reporting `worldPos.z=-48.4` — far outside any legitimate scene geometry (max possible Z for any body is 4, the back wall's outer face) — alongside a hit-rate drop from 96.1% (fix A+B only, but that rate was itself likely inflated by the pre-fix-C over-extension into the open face) down to 55.6%. All explicitly-sampled diagnostic pixels (the fixed 8-point sweep) report sane, in-bounds worldPos values, and independent numeric SDF simulation of the leftWall's exact bake parameters confirms the wall geometry itself is correctly bounded within the valid grid `[12,96]` (of `[0,112)`) — so this is NOT a re-diagnosis of root causes A/B/C, which are independently verified correct. (2) The screenshot's own framing/composition still doesn't read as a clean "camera looking into an open room" shot — it looks more like a close, oddly-angled view of a large blocky mass with visible mip/dither checkering, unlike the virtual variant's clean, well-framed result from the IDENTICAL shared camera preset (ruling out a camera-config bug). Given the virtual variant (same wall-span/asymmetry logic, different content-representation backend) is fully clean, this residual is most likely a baked-octree-path-specific rendering artifact (possibly related to the stray out-of-bounds hit, LOD/mip transition, or brick-boundary interaction) rather than a geometry-authoring bug — but this is a hypothesis, NOT a decisively-proven root cause the way A/B/C are. **Flagged as an honest residual for the next round rather than force-closed as DONE.**
  - **Verification method:** every fix in this round was verified with LIVE GPU evidence (HitRecord readback showing exact hit-rate and worldPos-range signatures before/after each fix, independent numeric SDF simulation of the actual bake parameters in Python) plus fresh screenshots actually looked at critically (corner-by-corner, not just "is there a recognizable box"), per this round's own brief — not just code-reading or trusting prior "APPROVED" verdicts.
  - Files changed: `application/main/source/graph/BuildRenderGraph.cpp` only (both `VIXEN_DDGI_CORNELL_BAKED_DEMO` and `VIXEN_DDGI_CORNELL_VIRTUAL_DEMO` blocks) — no shader changes.
  - **Status: DONE_WITH_CONCERNS.** The virtual variant meets this round's "coherent sealed room" bar cleanly. The baked variant is substantially improved (root causes A/B/C are real, verified, and fixed) but has an unresolved residual (stray out-of-bounds hit + non-clean framing) that a future round should investigate with fresh GPU diagnostics rather than assume is cosmetic.

- **M3 round 2 (2026-07-14): unified BOTH variants onto ONE shared world-space SdfInstruction source, per the user's own reframing of round 1's separate-symptom-fixing approach.** Round 1 fixed real bugs in each variant's OWN independently-authored geometry-construction path (baked: `gridBoxAt`, grid-local units pre-scaled by a subdivision factor; virtual: `worldBoxAt`, true world-space units) — two different transforms of the same `CornellBoxSceneDefinition.h` numbers, which the user correctly flagged as a real architecture smell and the likely reason the two variants exhibited two DIFFERENT symptoms in the first place (each transform could go wrong independently). This round removes that duplication entirely rather than just patching each transform's own bugs.
  - **Design:** `BuildCornellWorldSpaceBodies()` (new function, this file's anonymous namespace) is now the SINGLE source of all 8 bodies' `SdfInstruction` programs, authored ENTIRELY in true world-space units (position, half-extent — matching the virtual path's own simpler `worldBoxAt`/`worldSphereAt` convention, since the recipe-VM's `RoundedBox`/`Sphere` opcodes already carry an explicit position field and don't require grid-local indirection at the instruction level at all). The two backends now differ ONLY in how they get world-space sample points to `evalRecipe`:
    - **Virtual** already samples true world-space `rayOrigin` directly (`PROVIDER_PROCEDURAL`) — splices this exact program with zero transformation, genuinely unchanged from round 1's own geometry (same numbers, just sourced from the shared function instead of a local duplicate).
    - **Baked** gained `MakeCornellWorldSpaceEvalFn`-equivalent adapter logic (`makeWorldSpaceEval`/`bakeWorldSpaceBody` lambdas) that maps a raw bake-grid coordinate to true world space via EXACTLY the same formula the runtime shader's octree traversal already uses (`world = worldPos + (grid/n)*kWorldGridSize*renderScale`, i.e. `bodyWorldPos`/`bodyRenderScale`'s own convention, algebraically re-derived and verified this round: `world(p_raw) = bodyWorldCenter + (p_raw - n/2)/subdiv`), then evaluates the SAME shared world-space program directly via `BakeSdfWorld` (the fully-generic underlying bake primitive, not the `BakeRecipeInstructionsToSdfWorld` convenience wrapper round 1 used, which has a fixed `p-center` convention incompatible with this approach). This decouples brick-density/subdivision (a pure bake-grid-resolution choice — `n`/`subdiv` only affect `worldPos`/`renderScale`) from instruction authoring (now one shared source), resolving the apparent tension between round 5's own subdivision fix and "author once in world-space" — they are NOT actually in conflict once subdivision lives purely in the grid<->world mapping rather than being pre-baked into the recipe's own half-extents/positions the way round 1's `gridBoxAt` did.
  - **Verified NOT a regression:** rebuilt clean (`VIXEN` target, 0 failed, both a full incremental rebuild after the initial refactor and after fixing several stale-reference cleanup issues caught by the compiler/careful re-read, not runtime). Re-ran both variants fresh.
  - **Virtual variant: STILL CLEAN, unchanged correctness.** Fresh screenshot after unification is visually identical in character to round 1's own clean result — coherent sealed room, red/green walls, gray floor/ceiling/back panel meeting flush at every corner with no gaps, sphere/box objects correctly placed, ceiling light visible. Zero-bake proof re-confirmed (`allRegistered=true`, side-bake count=1 unchanged, not counted against the render-path proof). This confirms the refactor is behavior-preserving for the already-correct virtual path.
  - **Baked variant: IMPROVED further, but the SAME residual persists — now more precisely localized.** Fresh screenshot shows a large, coherent, connected faceted mass — red wall clearly visible on the left, green wall on the right, gray ceiling/back-wall panels forming what reads as a genuine corner-view into a box (a step up from round 1's own less-clean framing). Per-body diagnostics: all 5 walls baked with `brickCount=1176` (up from round 1's `784` — MORE occupied geometry correctly captured, consistent with the earlier `gridBoxAt` composition having still been imperfectly capturing the true wall extent even after round 1's own fixes). HOWEVER: a live HitRecord min/max scan still found a stray out-of-bounds hit (`worldPos.z=-48.9`, essentially unchanged from round 1's `-48.4` finding) and a hit rate of 104429/250000 (41.8%). **This is the single most useful finding of this round**: since the geometry SOURCE is now byte-identical between baked and virtual (literally the same `BuildCornellWorldSpaceBodies()` call), and the virtual path is clean while the baked path still shows this anomaly, the remaining bug is DEFINITIVELY NOT in geometry authoring/placement (round 1's own working hypothesis) — it must be in the baked/octree-SPECIFIC machinery: either the `BakeSdfWorld`/occupancy/brick-dilation mechanism, the octree traversal/AABB-cull shader path, or an LOD/mip-transition interaction, none of which the virtual/procedural path shares. This rules out an entire category of hypotheses in one comparison, exactly as the user's own reframing predicted a shared-source approach would do.
  - **Not yet root-caused:** this round did NOT chase the stray-hit anomaly further (time-boxed to the unification task itself, per its own scope) — a follow-up round should now search exclusively in baked-path-specific code (`BakeSdfWorld`'s occupancy/dilation passes, `BuildSdfBodyOctree`, `ConcatenateSdfWithMips`, or `TraceWorld.glsl`'s `PROVIDER_STORED` octree-traversal branch) rather than re-examining geometry authoring, which this round's own A/B comparison has now ruled out as the cause.
  - Files changed: `application/main/source/graph/BuildRenderGraph.cpp` only — added `BuildCornellWorldSpaceBodies()`/`CornellWorldBoxAt`/`CornellWorldSphereAt`/`CornellWorldSpaceBody` (anonymous namespace, shared by both demo blocks); baked block rewritten to consume it via `makeWorldSpaceEval`/`bakeWorldSpaceBody`; virtual block rewritten to consume it directly (recipe-id now derived as `2+i` from the shared body list's own fixed order, rather than hand-assigned per body). No shader changes.
  - **Status: DONE_WITH_CONCERNS (unchanged overall verdict, but with a materially better-localized residual).** Virtual variant: clean, unaffected. Baked variant: geometry-authoring correctness confirmed (ruled out as the cause of the remaining anomaly via direct A/B comparison against the now-identical virtual source) — the next round's search space is narrowed to baked-path-specific bake/traversal machinery.

- **M3 round 3 (2026-07-14): BLOCKED — localized the stray hit precisely, but it is a NON-DETERMINISTIC GPU synchronization bug, not a geometry bug, and requires deeper investigation than a screen-space diagnostic alone can resolve.** Per the user's follow-up request, extended the HitRecord readback (`VulkanGraphApplication.cpp`) to scan every pixel (not just an 8-point sample) for out-of-bounds `worldPos` and report the exact pixel + which instance won the hit — added `WorldHit.instIdx` (`TraceWorld.glsl`, both commit sites) threaded through `HitRecord._pad0[0]` (`BodyInstanceRayMarch.comp`, a real always-64-byte spare field, `test_hitrecord_sdi_parity.cpp`'s own layout assert is unaffected).
  - **Root cause is DECISIVELY localized, but NOT fixed:** two consecutive runs of the identical `VIXEN_DDGI_CORNELL_BAKED_DEMO=1` binary at tick 150 produced DIFFERENT results — run 1: 246279/250000 hit (98.5%), ZERO out-of-bounds hits. Run 2 (same binary, same scene, no code change between runs): 84026/250000 hit (33.6%), 1647/84026 out-of-bounds hits, first at pixel(152,128). **This is non-deterministic between runs of the identical binary — a genuine race condition or synchronization gap, not a deterministic geometry-authoring bug** (which M3 round 2's baked-vs-virtual A/B comparison had already ruled out as the remaining cause).
  - **Every single out-of-bounds hit in run 2 (20/20 sampled, capped at the diagnostic's own limit) hit `instIdx=2` (backWall) specifically** — a spatially contiguous cluster of pixels (rows 128-129, columns ~125-164), with worldPos X/Y staying plausible (X~8-22, Y~27-29) but Z going wildly negative (-20 to -48, impossible — no real geometry exists past Z=4). `hitT` values (85-114) are anomalously large vs. the ~25-40 range correct hits show. **Dividing the anomalous hitT by `kWallRenderScale` (2.8) lands almost exactly in the correct range** (110.58/2.8=39.5) — the exact numeric signature of a MISSING `hitT *= inst.renderScale` correction (the same bug class M1 rounds 4 and 6 already found and fixed at both of `TraceWorld`'s own call sites, and `TraceWorldShadow`'s). Both call sites were re-verified present and unmodified in the current code (`grep` confirms `hitT *= inst.renderScale;` at both `TraceWorld.glsl` lines 324 and 488) — so this is NOT a regression of that known fix; the correction code is present but appears to not be reliably APPLIED for `instIdx=2` on some runs.
  - **Live screenshot from the buggy run visually confirms the anomaly**: three separate white dots (duplicate/ghosted light-source artifacts) scattered around the frame, vs. a single coherent light source in the clean run — consistent with rays that should terminate on the back wall instead reading stale/wrong data and hitting something else entirely.
  - **Working hypothesis (NOT confirmed, needs further investigation by whoever picks this up next):** a race or missing synchronization barrier between the CPU-side `BodyOctreeSceneNode::SetInstances` upload (of the `bodyInstances` SSBO the shader reads `inst.renderScale` from) and the GPU's first read of that data on a given frame -- `BodyOctreeSceneNode.cpp` already has documented complexity around this exact area (own comments: "pointer-identity hazard keying inserts no barrier between them", references to a `vkCmdCopyBuffer`-related VUID). Specific to `instIdx=2` non-deterministically suggests either a per-slot upload-ordering issue or a genuine timing race that happens to manifest on that slot more often (worth checking if backWall's slot is written last/first in the upload sequence, or resides at a suspicious buffer offset).
  - **Why this was NOT chased further this round:** confirmed non-determinism (2 runs, same binary, different results) means the bug is in GPU synchronization/timing, a fundamentally different and harder investigation than the geometry-authoring bugs found in rounds 1-2 (which were static, 100%-reproducible, and fixable via pure CPU-side arithmetic). Properly investigating a race condition requires tools/techniques this round didn't have time to set up safely (e.g. repeated runs with validation-layer synchronization checks enabled, RenderDoc frame capture across multiple runs to diff GPU command submission order, or deliberately-inserted barriers to test the hypothesis) — attempting a blind fix (e.g. adding a barrier speculatively) risked either not fixing the real issue or masking it without understanding it, which the plan's own process discipline (live GPU evidence over guessing) argues against.
  - **Not yet done:** the actual synchronization root cause. A follow-up round should (1) run the baked demo 5-10 times back-to-back with this round's own OOB+instIdx diagnostic active, to establish the failure RATE (not just "it can happen") and check if it's specific to instIdx=2 consistently or shifts across runs; (2) inspect `BodyOctreeSceneNode.cpp`'s own instance-buffer upload/barrier code directly (the two comments this round found are a strong lead); (3) consider enabling Vulkan synchronization validation (`VK_LAYER_KHRONOS_synchronization2` or the validation layer's sync-val feature) to get a decisive answer instead of continuing to infer from symptoms.
  - Files changed: `application/main/source/VulkanGraphApplication.cpp` (extended CornellDiag readback: full-buffer OOB scan + instIdx reporting), `shaders/TraceWorld.glsl` (added `WorldHit.instIdx` field, populated at both nearest-hit commit sites), `shaders/BodyInstanceRayMarch.comp` (threads `worldHit.instIdx` into `HitRecord._pad0[0]`). This diagnostic instrumentation is left in place (cheap, real production fields, not disruptive scaffolding) rather than stripped, since it will be needed again by whoever picks up the synchronization investigation.
  - **Status: BLOCKED.** Not a weak/forced close — the geometry-authoring work (rounds 1-2) is genuinely done and verified. This round found real, decisive, reproducible evidence that the remaining bug is a non-deterministic GPU synchronization issue specific to instance index 2, several investigation leads, and left working diagnostic tooling in place — but did not fix it, since doing so responsibly requires a different kind of investigation (timing/sync, not geometry) than this round's own tools support.

- **M3 round 4 (2026-07-14): BLOCKED — tested and RULED OUT the leading hypothesis (lazy brick residency), found a new lead (screen-region correlation, not instance-specific), still not fixed.** Per the user's follow-up, ran the repeated-trial measurement round 3 had recommended, and pursued the strongest available lead from the codebase's own history.
  - **Repeated-trial measurement (round 3's own recommended next step):** ran the identical binary/scene 3 times back-to-back. Trial 1 (fresh diagnostic build): 98.5% hit, 0 OOB — clean. Trial 2: 33.6% hit, 1647 OOB, ALL on `instIdx=2` (backWall). Trial 3: 98.5% hit, 0 OOB, byte-identical numbers to trial 1 (`min=(-1.458727,-1.458727,18.785589)`) — clean again. **2 clean / 1 broken out of 3**, confirming the failure is real but intermittent, not universal.
  - **Hypothesis tested: lazy brick residency.** Found that `VIXEN_DDGI_CORNELL_BAKED_DEMO`'s scene block never calls `BodyOctreeSceneNode::RequestBrickResidency(true)` — since every one of its 8 bodies is mip-capable (`ConcatenateSdfWithMips` bakes+attaches a mip pool for each), `DeriveResidencyDefault` (`ResidencyDefault.h`) silently computes LAZY residency for the whole scene. This is the SAME class of bug an earlier, unrelated demo (`VIXEN_TIER_OBSERVABLE_DEMO`, M8 Task 21, `BuildRenderGraph.cpp` ~line 2854) had already hit and fixed with an explicit `RequestBrickResidency(true)` call, whose own comment describes exactly this failure mode: lazy residency means bricks populate via an ASYNCHRONOUS background upload (`PollBrickUploadCompletion`, "a multi-frame latency between residency requested and brickResident actually visible on GPU is expected and correct") — a very plausible mechanism for "sometimes correct, sometimes not" behavior depending on whether that async upload caught up before tick 150's diagnostic read.
  - **RULED OUT by direct live test.** Added the identical `RequestBrickResidency(true)` call to the Cornell baked block, rebuilt, ran fresh. Result: STILL broken — 7894/191705 OOB hits (a WORSE ratio than trial 2's lazy-residency failure), min worldPos.z=-51.2 (further out of bounds than round 3's -48.9). **Critically, the culprit instance shifted from `instIdx=2` (backWall, every prior trial) to `instIdx=4` (ceiling)** — proving the anomaly is not tied to residency state, and not tied to one specific body's data either. The eager-residency change was reverted (kept only as an explanatory code comment marking the hypothesis as tested-and-disproven, so a future investigator doesn't retry it) rather than left in place as an unverified/disproven "fix."
  - **New lead found while reviewing the disproof: the two broken trials' out-of-bounds pixel clusters land in roughly the SAME screen region** (trial 2: rows 128-129, columns ~125-164; the residency-test trial: row 123, columns ~155-170+) — both in the upper-left-of-center area, near where the ceiling and back-wall's screen-space silhouettes meet. Combined with the culprit INSTANCE shifting between trials (backWall, then ceiling) while the screen REGION stayed roughly consistent, this suggests the common factor may be the SCREEN-SPACE/ray-geometry situation at that specific pixel range (e.g. rays nearly grazing the exact seam between two instances' AABBs, a degenerate camera-ray angle for that dispatch tile) rather than a specific body's own data being stale — a materially different, not-yet-tested hypothesis for the next round.
  - **Also traced (not yet tested):** `BodyOctreeSceneNode::DeriveShellCache`/the "Surface-Shell ESVO cache" double-buffer mechanism (`shellCache_[0]`/`[1]`, `shellDataBuffer_[readSlot/writeSlot]`, `ExecuteImpl`'s own `rawFrame&1` slot selection) runs unconditionally whenever the scene has real SDF channel data (true for this scene) — a genuinely complex, actively-double-buffered derivation this round did not have time to fully trace, and a plausible alternative locus for the non-determinism given its own explicit read/write-slot-hazard reasoning in the source comments.
  - **Why still not fixed:** this round substantively progressed the investigation (ruled out one concrete, plausible, evidence-backed hypothesis with a real live test rather than reasoning alone; found a new spatial-correlation lead that reframes the search from "which body" to "which screen region/ray geometry") but did not reach a confirmed root cause. Given the residency hypothesis's disproof revealed the bug is more subtle than initially evidenced (instance-shifting, not residency-tied), continuing to guess at fixes risks more wasted cycles than the value of stopping to report precisely, per this round's own standing discipline.
  - Files changed: `application/main/source/graph/BuildRenderGraph.cpp` only (the tested-and-reverted `RequestBrickResidency(true)` call, replaced with an explanatory comment recording the disproof so it isn't retried blind).
  - **Status: BLOCKED (evidence substantially deepened, root cause still not found).** The geometry-authoring work (rounds 1-2) remains genuinely DONE. This round's repeated-trial data, the residency hypothesis's clean disproof, and the new screen-region correlation lead are all real, verified findings for whoever continues this investigation — but the actual fix requires either the shell-cache trace this round didn't complete, or GPU-side tooling (sync validation layers, RenderDoc capture diffing) beyond what CPU-side readback diagnostics alone can resolve.

- **M3 round 5 / combined-verify (2026-07-15): ROOT-CAUSE FOUND + FIXED — the deterministic "fragmented Cornell box" was THREE stacked baked-path bugs, none of which the prior rounds' aggregate-numeric readback could see. Baked variant now renders a correctly-laid-out room.** Ran in a fresh worktree combining `main`'s render-graph reuse-while-pending sync fix (commit `1c5e3836`) with `fix/cornell-geometry-coherence`'s geometry+world-space-unification fixes (`b0ebd4cf`) — the exact combination the earlier BLOCKED rounds never had together. The key methodological change this round (per the controller's explicit instruction): **prioritize direct VISUAL diagnosis over aggregate hit-rate/min-max stats**, because those aggregates repeatedly reported "clean" (98.5% hit, worldPos in-bounds) while the actual per-pixel render was structurally broken. Replaced the handful-of-sample-pixels readback with a **coarse-grid (25×25, every-20th-pixel) ASCII map of the winning instIdx + a worldPos.z depth map** — this immediately showed the render was incoherent in a way no scalar ever could: bodies winning the nearest-hit in geometrically impossible screen regions (center-of-screen hitting leftWall at z=27; worldPos.z NEVER below ~18.8, i.e. no ray ever reaching the interior/back wall), even though the aggregate "246279/250000 hit, 0 out-of-bounds" looked perfectly healthy. The clean VIRTUAL variant (same shared `BuildCornellWorldSpaceBodies()` source) was used as the control throughout: it renders a textbook Cornell box, proving the bug is baked-octree-path-specific, not geometry authoring.
  - **Root cause #4 (the deepest, fixed in `BuildRenderGraph.cpp`): `kWallN` was NOT a power of two.** `BuildSdfBodyOctree` (`SdfBake.h`) derives the octree's own voxel resolution as `2^(floor(log2(n)) + brickDepth - brickDepth)` — i.e. it silently ROUNDS `n` DOWN to the previous power of two. `kWallN=112` (and the geometry rounds' earlier `96`) are not powers of two, so the octree was built at a **64-voxel** grid while the SDF was baked into a **112-voxel** grid. The bake's `bricksPerAxis` (14) and the octree's own `bricksPerAxis` (8) DISAGREE → the shader's brick/voxel pool addressing reads the wrong cells → every body's occupancy is misplaced/misattributed across the frame (the "which body renders where is scrambled" symptom the coarse-grid instIdx map exposed). **Fix: `kWallN` 112 → 128** (next power of two ≥ the required 104-cell span), so the octree grid EQUALS the bake grid and addressing is consistent end to end. After this fix the coarse-grid instIdx map is textbook-correct: leftWall(0) fills the left columns, rightWall(1) the right, ceiling(4) the top band, floor(3) the bottom, backWall(2) the center, light(5) the center panel, sphere(6)/box(7) inside — and the depth map shows the back wall correctly deepest (z<6) in the center with side walls receding. This is the "leftover interaction between the world-space-unification refactor and the baked traversal path" the geometry rounds flagged (their hypothesis (c)) but never pinned: rounds 1/5 set `n` to non-pow2 values for brick-alignment without noticing `BuildSdfBodyOctree`'s pow2 contract.
  - **Root cause #5 (fixed in `BuildRenderGraph.cpp`): the scene boots LAZY brick residency.** Every body is mip-capable → `DeriveResidencyDefault` returns lazy → the primary visible hit resolves via `shadeFromMipSample` (`MipFallback.glsl`) at coarse leaf-NODE granularity: grey base color, flat up-normal, leaf-entry `t` — a coarse mip approximation, not the real trilinear brick surface. That is a large part of the "dithered/fragmented/grey-block" look. **Fix: `RequestBrickResidency(true)`** so the primary hit brick-marches the real SDF iso-surface (`handleLeafHitInstancedSdf`) — the same fidelity the virtual variant gets analytically. (This is what M3 round 4 tried and wrongly "ruled out" — see #6 for why that trial's residency never actually stuck.)
  - **Root cause #6 (fixed in `VulkanGraphApplication.cpp`): a per-FRAME residency heuristic silently stomped the grant.** `UpdateBodySceneResidency()` runs every tick and calls `RequestBrickResidency(anyInstanceWantsBricks)` from a frustum/pixel-resolvability trigger TUNED FOR THE ORBIT-SCALE TIERED-ESVO DEMOS. For this fixed close box the walls fail its pixel-size threshold, so it re-decided residency to **false every frame**, overriding the BuildRenderGraph-time `RequestBrickResidency(true)`. This is exactly why M3 round 4's residency trial "still produced out-of-bounds hits" — its grant never survived to the render (a live diagnostic this round added, `[CornellDiag] residency at tick 150`, showed `requested=false brickPoolUploaded=true`: the confused half-granted state that heuristic produces). **Fix: `UpdateBodySceneResidency` early-returns for `VIXEN_DDGI_CORNELL_BAKED_DEMO`**, exactly like `VIXEN_TIER_ZOOM_DEMO`'s own scripted-residency ownership, leaving the demo's explicit grant the exclusive driver. After the fix the same diagnostic reads `requested=true brickPoolUploaded=true`.
  - **Verification — VISUAL, not just numeric (the whole point of this round).** Fresh `VIXEN_DDGI_CORNELL_BAKED_DEMO=1` capture (tick 150) actually looked at: a coherent Cornell box — red left wall on the left, green right wall on the right, gray back/floor/ceiling meeting at correct corners at the right scale, the ceiling light in the center, sphere + box objects inside. Confirmed deterministic (two back-to-back runs byte-identical in the full HitRecord scan). The coarse-grid instIdx + depth maps (above) independently confirm every body projects to its geometrically-correct screen region and depth — a decisive, reconstruction-level check the prior aggregate stats could never provide. The virtual variant was re-run in the same build and confirmed unchanged/clean (my changes are all `VIXEN_DDGI_CORNELL_BAKED_DEMO`-gated), so no regression to the working path.
  - **Honest residual (DONE_WITH_CONCERNS, not a clean close):** ~22% of hits still flag the generous `[-20,60]` out-of-bounds box, concentrated as grazing rays over the walls'/ceiling's still-OVERSIZED per-body AABBs (n/subdiv = 32³ world units for a thin wall) that brick-march to a far ghost surface (hitT~76, worldPos far behind the box). These no longer scramble the visible layout (the pow2 fix corrected addressing) but leave two dark rectangular voids and some residual softness/DDGI-gather noise vs. the virtual variant's cleaner flat shading. The clean next step is TIGHT per-body bake AABBs (a box-shaped grid hugging each thin wall, not a cube) so grazing rays don't traverse large empty regions — deliberately NOT attempted this round, since it is a real bake-grid redesign (the octree builder currently assumes a cube grid) and the three fixes above already convert the render from "structurally broken fragments" to "a correct, recognizable Cornell box," which was the reported blocker.
  - Files changed: `application/main/source/graph/BuildRenderGraph.cpp` (`kWallN` 112→128 pow2; `RequestBrickResidency(true)`), `application/main/source/VulkanGraphApplication.cpp` (`UpdateBodySceneResidency` demo skip; coarse-grid instIdx/depth diagnostic + residency-state log left in place behind the demo's tick-150 gate for the next investigator).
  - **Status: DONE_WITH_CONCERNS.** The reported structural corruption (fragmented/scrambled/incoherent baked box) is root-caused and fixed with decisive visual + per-region evidence; the baked variant now renders a correctly-laid-out Cornell box. The remaining grazing-ray far-hit residual (oversized AABBs) is precisely characterized with a concrete fix direction, but left for a follow-up rather than force-fixed in the bake-grid code this round.

- **M3 round 6 / combined-verify (2026-07-15): INVESTIGATED the round-5 residual under the "fix the `marchBrickSdf` grazing-ray arc-length overcount" framing — and DECISIVELY DISPROVED that framing. No shader code changed (correctly): the overcount is NOT in `marchBrickSdf`'s seam-continuation math, and the residual is a shading-quality gap, not visible geometry corruption.** Per the controller's explicit rigor bar, derived the `sBase`/`sEnter` seam-continuation math by hand BEFORE touching code, then confirmed it with two decisive live experiments rather than patching on a hunch. The controller's own standing permission — "if the derivation reveals this is riskier or larger than expected, stop and report precisely rather than push through" — is exactly what happened: the derivation + experiments showed the proposed fix target is wrong.
  - **Hand-derivation of the seam continuation (`StoredSdf.glsl` lines 456-473).** `sEnter = max3(ntlo)` is the standard slab BOX-ENTRY test in `nextBrick`'s frame; `sBase` accumulates it globally so `sHit = sBase + s` is one consistent arc-length. Hand-checked two cases: (1) straight-on `gridDirN=(1,0,0)` entering the adjacent brick — the `axisParallel` guard (`≤1e-8`) sends the two zero-direction axes to `-64` (ignored by `max3`), leaving `sEnter=8` (correct); (2) grazing `gridDirN≈(0.9998,0.02,0)` — for the TRUE adjacent leaf the traversal steps to, the near-parallel axis contributes a large NEGATIVE `ntlo` (also correctly ignored by `max3`), and `sEnter` still equals the geometrically-correct box entry. **Conclusion of the derivation: the slab math agrees with the traversal's own `state.t_min` whenever they agree on the brick — i.e. the "grazing `max3` picks a huge near-parallel value" hypothesis from round 5's residual note is WRONG on close inspection** (the `axisParallel` guard + `max3`'s own sign handling already neutralize the near-parallel axis).
  - **Decisive experiment A — `MAX_BRICK_HOPS` capped 2048→2 (shader-only, runtime-recompiled, no rebuild — shaders compile at runtime from the worktree's own `VIXEN/shaders`, confirmed via the baked-in `VIXEN_SHADER_SOURCE_DIR` string in the binary).** Result: OOB count **byte-identical to the uncapped run (40081/183556), pixel(125,125) hitT=76.095 unchanged.** → The ghost hits do NOT require deep hopping; the inflation is fully produced within the FIRST brick-exit's continuation, not accumulated across many bricks. Rules out the "deep-hop / trilinear false-positive across many bricks" mechanism.
  - **Decisive experiment B — `MAX_BRICK_HOPS` capped to 1 (single brick, NO seam continuation, NO `sEnter` recomputation at all).** Result: OOB count DROPPED but did NOT vanish — **29517/117520 still OOB**, first at pixel(143,143). → A large fraction of the "ghost" hits occur inside the FIRST brick's own sphere-trace, with the `sEnter` seam-continuation code never executed. **This definitively excludes `sEnter`/`marchBrickSdf`'s seam math as the cause** (the originally-proposed fix target would have had zero effect on those 29517 hits). The remaining hops-2-vs-1 delta (40081−29517≈10.5k) is legitimate seam-crossing hits into adjacent bricks that hops=1 turns into false MISSES (visible as black voids in the hops=1 screenshot), i.e. the continuation is doing correct work, not creating ghosts.
  - **What "OOB worldPos" actually is.** `HitRecord.worldPos = rayOrigin + rayDir * worldHit.t` (`BodyInstanceRayMarch.comp:246`) — a faithful readout of `hitT`, so OOB worldPos ⟺ inflated `hitT`, the SAME evidence, not independent. And `hitT = tBias + state.t_min + sHit/(dirLen·gridScale)` with `sHit ≤ 8√3 ≈ 14` voxels and `gridScale = bpa·8 = 128`, so the `sHit` term is tiny — the inflation lives in **`tBias + state.t_min`** (the ESVO entry parameter of the brick the traversal handed the march), NOT in the arc-length `sHit`. The march is reporting a legitimate iso-crossing inside a brick the grazing ray reaches only at large ray-parameter; over the walls'/ceiling's still-OVERSIZED per-body AABBs (round 5's residual #), a grazing ray traverses a long empty span before crossing → large `state.t_min` → worldPos pushed far behind the box. This is the round-5 residual restated precisely, and its true fix remains **tight per-body bake AABBs** (the octree builder's cube-grid assumption, explicitly left as future work), NOT a `marchBrickSdf` change.
  - **VISUAL verification (the controller's top directive) — baked vs virtual at the same tick 150 / same camera.** Baked (`hops=2048`, production): a coherent, correctly-laid-out Cornell box — red left wall, green right wall, floor/ceiling/back-wall at correct corners, ceiling light dot, and the two objects (sphere region + floor box) in place; the coarse-grid instIdx map is textbook-correct and the depth map is spatially COHERENT (no scattered random ghost pixels — the 35-40k "OOB" pixels cluster in the dim interior, they are not visible corruption). Virtual (procedural control): a cleanly LIT Cornell box with grey walls, a clearly-shaded sphere and box on the floor, even lighting. **The honest gap between them is SHADING QUALITY, not geometry:** in the baked variant the two objects render near-BLACK (unlit), there is heavy purple/blue/green DDGI color-bleed across the walls, and the whole room is dimmer. Geometry placement matches virtual; shading does not.
  - **Honest answer to "does the DDGI shading gap improve as a side effect?" — NO, and it could not have**, because no fix was applied (the proposed one was disproven). The dark-objects + purple-bleed + dimness gap is real and stable across ticks, and is a SEPARATE follow-up from anything in `marchBrickSdf`. Best current read: it stems from (a) the oversized-AABB grazing far-hits still feeding some wrong-depth/wrong-normal samples into the DDGI gather, and (b) the baked path's coarser per-voxel normals/color vs the virtual path's analytic surface — i.e. it wants the tight-AABB bake redesign AND a look at the DDGI gather's tolerance to baked-path hit quality, not a brick-march arithmetic tweak.
  - **Spot-check of OTHER stored-SDF scenes: N/A this round** — since NO shared shader code was changed (the disproof means `StoredSdf.glsl` is byte-identical to round 5's committed state), there is no shared-code regression surface to check. All diagnostic edits (`MAX_BRICK_HOPS` 2 then 1) were reverted; `git diff` on `shaders/` is empty.
  - Files changed: **NONE** (working tree clean; the two `MAX_BRICK_HOPS` diagnostic edits were made, measured, and fully reverted). The round-5 diagnostic instrumentation (coarse-grid map, OOB scan, residency log) was reused as-is.
  - **Status: DONE (as an investigation) / the round-5 residual remains open with a CORRECTED root-cause direction.** Deliverable this round is the decisive disproof of the `marchBrickSdf` fix target + the corrected characterization: the residual is (1) large-`state.t_min` grazing far-hits over OVERSIZED per-body AABBs → fix is TIGHT per-body bake AABBs (octree cube-grid redesign, unchanged as future work), and (2) a distinct DDGI shading-quality gap (dark unlit objects, purple bleed) that is its own follow-up. Recommend NOT reopening the `marchBrickSdf` seam-math angle — it is now experimentally excluded.

- **M3 round 7 (2026-07-15): TWO NEW user-caught bugs root-caused with decisive evidence — (1) garish incoherent baked wall colors, (2) a ~147x perf collapse (baked 0.9 FPS vs virtual 126 FPS). Both reproduced fresh on this machine; neither is the same as the already-documented deferred issues, though bug 2 shares a root with the oversized-AABB item.** Fresh worktree `fix/cornell-baked-color-perf` off `main` `25fdbdca` (has round-5's pow2+residency fixes + the discrete-GPU-selection fix). Measured, not estimated: virtual `avg 7.935 ms (126.0 FPS)`, baked `avg 1168.349 ms (0.9 FPS) | p99 3804.635 ms` — same build, same camera, same tick-150 capture path.
  - **Bug 1 has TWO independent facets, disentangled with live pixel evidence:**
    - **1a — CAPTURE-ONLY R↔B swap (not an on-screen bug).** The virtual variant's screenshot shows a BLUE left wall, but `kLeftWallColor` is authored RED `(0.75,0.15,0.15)`. Root cause: `CaptureSwapchainToPng` (`RenderGraph/include/Debug/RenderTargetReadback.h`, the readback behind `CaptureHudFrameToPng` — the path ALL `VIXEN_HUD_CAPTURE_FRAMES` PNGs use) copies channels straight through (`rgb[0]=rgba[0]`…) with NO BGRA→RGBA swap, UNLIKE `FrameCapture.cpp:438` which correctly `std::swap(pixels[i*4+0], pixels[i*4+2])`. On this WSL2/Dozen(D3D12) box the swapchain is `VK_FORMAT_B8G8R8A8_UNORM` (BGRA — see `VulkanSwapChain.cpp`'s own SRGB→UNORM Dozen fix), so the raw bytes are B,G,R,A and the PNG mislabels B as R. **Proven by pixel sampling** the virtual PNG: left-wall `(11,11,57)` (B-dominant → unswaps to `(57,11,11)`=red ✓), right-wall `(11,50,11)` (G-dominant, symmetric under R↔B → stays green ✓), floor/back near-gray (near-symmetric ✓). No blue is authored anywhere in `CornellBoxSceneDefinition.h`. The on-screen present is CORRECT (format-aware `imageStore`+present); only the PNG readback is wrong. Real bug, worth fixing (the capture is the investigation's own instrument), but it is NOT the reported on-screen garish coloring.
    - **1b — BAKED per-voxel RAINBOW color (real, on-screen).** The baked screenshot shows garish red/yellow/pink/purple/orange blotches with no flat per-wall color. Root cause: `BakeSdfWorld` (`SVO/include/SdfBake.h:215`) hardcodes a per-voxel cosine-rainbow into the SEM_COLOR channel (`col = 0.5+0.5*cos(p*0.12 + {0,2.094,4.188})`, the classic IQ 0/120/240° palette). The STORED shading path multiplies it by the instance tint — `TraceWorld.glsl:330 bestColor = hitColor*inst.color` where `hitColor = sampleChannelVec3Trilinear(SEM_COLOR,…)` (`SceneBindings.glsl:527`) — so each wall renders `rainbow(pos) × tint` = blotches. The PROCEDURAL/virtual path has no baked voxel channel and uses flat `inst.color` (`TraceWorld.glsl:167`), so it renders coherent flat per-wall color. Both C++ blocks feed the SAME correct authored per-body color into `BodyInstanceGpu.color` (baked `makeInstance(…, bodies[i].color, …)`; virtual `inst.color=b.color`) — the divergence is purely the baked path's extra per-voxel SEM_COLOR multiply. NOTE: `test_body_instance_raymarch_render.cpp:1172` asserts this exact rainbow formula, so the shared `BakeSdfWorld` MUST NOT be changed — the fix must overwrite the Cornell bodies' SEM_COLOR channel to a flat color scene-locally (the SAME pattern `VIXEN_TIER_CROSSING_DEMO` already uses to force solid magenta, `BuildRenderGraph.cpp:1399-1418`).
  - **Bug 2 — the ~147x perf collapse is GPU-compute-bound in the STORED octree trilinear brick-march, NOT any of the CPU/residency suspects, and NOT the grazing-ray deep-hop cost.** Ruled out decisively:
    - **NOT CPU-side.** The GaiaVoxelWorld bakes (`754688`-entity batches) all complete BEFORE "Entering render loop" (one-shot startup, not per-frame). `BodyOctreeSceneNode::ExecuteImpl` per-frame work is a tiny instance-list memcpy (no shell re-derive: `dirtyBricks_` empty; residency one-shot). The `[debug_capture] Exported 256 ray traces to JSON` line fires every frame but is IDENTICAL in the 126-FPS virtual run, so it is not the differentiator. Residency is stable-eager (`[CornellDiag] residency at tick 150: requested=true brickPoolUploaded=true`) — `UpdateBodySceneResidency` correctly early-returns for this demo, no per-frame re-upload.
    - **NOT grazing-ray deep hops.** Shader-only experiment (runtime recompile, no rebuild): `MAX_BRICK_HOPS` 2048→8 left avg essentially UNCHANGED (`avg 951.938 ms (1.1 FPS)`) — only p99 improved (3804→1192 ms). So deep hopping over the oversized AABBs causes the frame-time SPIKES, but the BASELINE ~1-FPS cost is not hop depth.
    - **IS the STORED-vs-PROCEDURAL traversal cost × ray volume.** The ONLY difference between the two variants is `providerKind` (0=STORED brick-march vs 1=PROCEDURAL analytic) — same graph, same probe grid (both force-enable `VIXEN_PROBE_GRID_CONFIG_ENABLED=1`, so probes are NOT a confound), same DDGI passes. The STORED path pays a full ESVO descent + trilinear `sampleSdfTrilinear` (8 texel fetches/step) + `sdfGradientStored` (6 more/hit) per ray, across ~250k primary rays PLUS the ProbeUpdate pass's 512 probes × raysPerProbe rays, every frame, over 8 heavily-overlapping full-resolution 128³ octrees. The PROCEDURAL path is one analytic `evalRecipe` (a single RoundedBox distance) per step with tight bound spheres — ~147x cheaper. This is the perf CONSEQUENCE of the deferred oversized-cube-AABB item (#1 in the handoff) plus round-5's `RequestBrickResidency(true)` (which forces the expensive full brick-march instead of the cheap mip-fallback), but its magnitude was never measured before this round.
  - **Not yet done in this entry:** the fixes for 1a/1b and the perf mitigation (see the round-7 fix entry / handoff update below once landed). Perf's true fix is the deferred tight-AABB / resolution / residency redesign — a targeted patch cannot close 147x without it.

- **M3 round 7 FIX (2026-07-15): both color facets FIXED + verified; perf DEFERRED with decisive evidence (DONE_WITH_CONCERNS).**
  - **Fix 1a (capture R↔B swap) — `RenderTargetReadback.h`.** Made `CaptureSwapchainToPng` (and its sibling `CaptureRenderTargetToPng`) FORMAT-AWARE: swap the R/B bytes when `target->GetFormat()` is a `VK_FORMAT_B8G8R8A8_*` family format (the Dozen/D3D12 swapchain format on this box), matching the swap `FrameCapture.cpp` already does. No-op on RGBA targets (so the editor/HUD capture tests that read RGBA offscreen targets are byte-unchanged). **Verified:** fresh virtual capture now shows a RED left wall (pixel `(57,11,11)`, R-dominant — was `(11,11,57)` blue) and GREEN right wall; floor/back gray. The on-screen present was always correct; this only fixes the captured PNGs (the investigation's own instrument).
  - **Fix 1b (baked rainbow) — `SdfBake.h` + `BuildRenderGraph.cpp`.** Added an OPTIONAL `ColorFn` template parameter to `BakeSdfWorld` (7th arg, defaults to `DefaultBandColor` = the pre-existing debug rainbow, so every other caller AND `test_body_instance_raymarch_render.cpp`'s rainbow assertion are byte-unchanged). The Cornell baked block now bakes FLAT WHITE `(1,1,1)` into every body's SEM_COLOR channel. Rationale: the STORED shading path does `bestColor = hitColor * inst.color`, so a white voxel color makes it `1 * inst.color = inst.color` — EXACTLY the flat per-wall tint the virtual/PROCEDURAL path already renders (`bestColor = inst.color`). Baking white (not the tint) avoids squaring the color, since the tint is already applied once via `inst.color`. Baking it via `ColorFn` (vs a post-bake channelPool overwrite) fixes the brick pool AND the mip pool consistently. [VERIFY PENDING baked re-run: expect flat red/green/gray walls, no rainbow blotches.]
  - **Bug 2 (perf) — DEFERRED, decisively root-caused (see the round-7 investigation entry above).** The ~147x is the inherent GPU cost of the STORED trilinear brick-march × ~250k primary + tens-of-thousands of probe rays/frame over 8 overlapping full-resolution 128³ octrees; NOT CPU-side, NOT grazing-ray deep-hops (capping `MAX_BRICK_HOPS` 2048→8 left avg unchanged at ~1 FPS). Its true fix is the SAME deferred redesign as OUT-OF-SCOPE issue #1 below (tight per-body box-shaped AABBs / lower bake resolution / fewer resident overlapping bodies) — a targeted patch cannot close 147x. Candidate mitigations considered and rejected as unsafe-without-a-redesign: lowering `kWallN` below 128 breaks the pow2 coverage; lowering `subdiv` risks reintroducing round-5's thin-wall dilation. The color fixes do NOT change perf (bake-time only).
  - Files changed this round: `libraries/RenderGraph/include/Debug/RenderTargetReadback.h` (format-aware R↔B swap, both capture helpers), `libraries/SVO/include/SdfBake.h` (additive `ColorFn` param + `DefaultBandColor` extraction), `application/main/source/graph/BuildRenderGraph.cpp` (Cornell baked block bakes flat white).

---

## Baked Cornell Box — Status Summary & Handoff (2026-07-15, after M3 rounds 5-6)

This section is the clean, current-state summary the investigation thread landed on. Read this first; the round-by-round log above is the supporting evidence. **This thread is CLOSED as of 2026-07-15** — the structural win is banked; the two remaining issues are architecturally deeper than a targeted bug fix and are deferred, not open work items.

### DONE (shipped, committed `3b497967`; verified visually)
- **The baked (`VIXEN_DDGI_CORNELL_BAKED_DEMO`) variant now renders a CORRECT, COHERENT, recognizable Cornell box** — red left wall, green right wall, gray back/floor/ceiling meeting at the right corners and scale, ceiling light, and both objects (sphere + box) placed correctly. This was the reported blocker ("fragmented/scrambled/unrecognizable geometry") and it is fixed. Deterministic across runs; the coarse-grid instIdx map and depth map both confirm every body projects to its geometrically-correct screen region.
- **Three stacked root causes fixed** (details in the round-5 log entry): (#4) `kWallN` was not a power of two, so `BuildSdfBodyOctree` silently built a 64-grid octree under a 112-grid bake → mismatched brick addressing → scrambled occupancy. Fixed 112→128. (#5) the scene booted LAZY brick residency → coarse mip-fallback shading instead of the real trilinear surface. Fixed with `RequestBrickResidency(true)`. (#6) a per-frame residency heuristic (`UpdateBodySceneResidency`, tuned for orbit-scale tiered-ESVO demos) stomped that grant every tick. Fixed by early-returning for this demo.
- Files carrying the fix: `application/main/source/graph/BuildRenderGraph.cpp`, `application/main/source/VulkanGraphApplication.cpp` (the latter also carries the coarse-grid instIdx/depth + residency-state diagnostic, left in place behind the demo's tick-150 gate for future work).

### OUT OF SCOPE (deferred — deliberately NOT fixed on this thread, with the reason)
Both remaining issues are real but need architectural work, not a targeted patch. Neither degrades the geometry — the box is structurally correct; these are shading/quality gaps vs the virtual reference.

1. **Oversized cube-shaped per-body bake AABBs → grazing far-hits.** Each body is baked into a CUBE grid (`BakeSdfWorld`/`BuildSdfBodyOctree` take a single `int n` and assume a cube), so a thin wall gets a fat `n/subdiv`-unit cube of mostly-empty space around it. A grazing ray traverses that empty span before crossing the iso-surface, so the ESVO brick-entry parameter `state.t_min` (and thus `hitT`/`worldPos`) is legitimately large — the ~35-40k "out-of-bounds worldPos" hits the diagnostic counts. **These are NOT visible corruption** (they cluster coherently in the dim interior; the render still reads as a correct box) and are **NOT a `marchBrickSdf` arc-length bug** — round 6 experimentally disproved that (the inflation is in `state.t_min`, not the march's `sHit`; `MAX_BRICK_HOPS` cap-to-1 left most of them intact). **True fix: TIGHT, per-axis (box-shaped) per-body bake grids** so each thin wall's grid hugs its own extent. This is a real redesign of the bake→octree→(serialize)→shader chain — the cube-grid assumption is baked into `BakeSdfWorld`'s single-`int n` signature, `BuildSdfBodyOctree`, `SVORebuild`'s `voxelsPerAxis = int(worldSize.x)`, and the pow2 grid contract. Do NOT reopen the `marchBrickSdf` seam-math angle — it is experimentally excluded. **PERF UPDATE (M3 round 7, 2026-07-15):** this oversized-AABB + full-resolution-brick-march configuration is ALSO the root of a measured ~147x per-frame perf collapse (baked 0.9 FPS vs virtual 126 FPS) — decisively GPU-bound in the STORED traversal path, not CPU/residency, not grazing-ray deep-hops (see the round-7 investigation entry). So this redesign now has a perf motivation, not just a hit-quality one; the same tight-AABB / lower-resolution / fewer-resident-bodies direction addresses both.

2. **DDGI shading-quality gap (dark unlit objects + purple/blue/green color bleed + overall dimness).** Side-by-side at the same tick/camera, the baked box's two objects render near-BLACK (unlit) with heavy DDGI color-bleed across the walls, while the virtual (procedural) control is cleanly lit with a clearly-shaded sphere and box. This is a SEPARATE follow-up and **may be partly downstream of issue #1** (the oversized-AABB grazing far-hits feed some wrong-depth/wrong-normal samples into the DDGI probe gather), plus the baked path's inherently coarser per-voxel normals/color vs the virtual path's analytic surface. Investigating it likely wants issue #1 fixed first, plus a look at the DDGI gather's tolerance to baked-path hit quality (normal/depth error). Not a brick-march or geometry-authoring issue.

### For whoever picks this up next
- Start from the virtual variant as the visual/lighting reference (`VIXEN_DDGI_CORNELL_VIRTUAL_DEMO=1`) — same shared `BuildCornellWorldSpaceBodies()` geometry source, cleanly lit, so any baked-vs-virtual gap is baked-path-specific.
- The tick-150 diagnostic (coarse-grid instIdx map + worldPos.z depth map + full-buffer OOB scan + residency-state log, in `VulkanGraphApplication.cpp`) is the right instrument and is left in place. **Judge the render by the maps/screenshot, not the aggregate OOB count** — that count flags coherent dim-interior far-hits that are not visible corruption, and chasing it as a bug is what round 6 disproved.
- Runtime note: shaders compile at runtime from the worktree's own `VIXEN/shaders` (the absolute path is baked into the binary as `VIXEN_SHADER_SOURCE_DIR`), so a shader-only experiment does NOT need a rebuild — edit the `.glsl` and re-run. Demo startup is heavy (~5-7 min: the GaiaVoxelWorld ECS bake runs several times before first render), so budget the wait and poll on an interval; the capture lands at tick 150.
