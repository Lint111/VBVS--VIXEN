# SDF Bake Box-Tight Region — Plan (2026-07)

**Trigger:** Cornell Box GI demo investigation (2026-07-14/15) found `BakeSdfWorld`/`BuildSdfBodyOctree` bake every body into a CUBE grid (single scalar `int n`, identical on all 3 axes), and confirmed this is a genuinely prohibiting problem, not just a cosmetic one: a thin body (a Cornell wall, ~2 world-units thick but 20+ units wide/tall) must be baked into a cube big enough to cover its LONG axis — wasting enormous empty volume on its THIN axis. This produces two separately-measured real costs on the SAME root cause: (1) grazing rays traverse the wasted empty span before reaching the iso-surface, reporting a legitimate-but-large `hitT`/`worldPos` (the "~35-40k out-of-bounds hits" finding from the Cornell investigation's M3 rounds 5-6 — NOT visible corruption, but real wasted traversal); (2) a decisively measured **~147× GPU performance collapse** (baked Cornell scene ~0.9 FPS vs. an equivalent procedural/analytic scene ~126 FPS, M3 round 7) — the GPU pays full trilinear brick-march cost across mostly-empty cube volume, for every thin body, every frame, for both primary rays AND DDGI probe rays. This blocks the baked Cornell variant from being usable and is a real cost tax on ANY thin/elongated stored-SDF body in this engine, not Cornell-specific.

**Base:** `main` (current HEAD `7b40ff0e` at plan-writing time — has the render-graph sync fix, the Cornell geometry+baked-path fixes, the discrete-GPU-selection fix, and the Cornell color fixes all merged).

## Grounding from codebase investigation (2026-07-15, via a dedicated Explore survey)

**The anisotropy machinery already exists end-to-end — this is what makes the fix narrow, not a ground-up redesign.** The GPU config struct (`OctreeConfig`) already carries a genuine per-axis AABB (`gridMin`/`gridMax` as `Float3`, `OctreeConfig.cs:...`, byte offsets 32/48) and a full `localToWorld`/`worldToLocal` `Mat4` (non-uniform-scale-capable) at byte offsets 64/128. Every shader consumer of world↔local transforms already applies the FULL matrix (`ESVOCoefficients.glsl:53,57`; `SceneBindings.glsl:476`) — there is no scalar shortcut anywhere in the traversal math that assumes uniform scale. `rayAABBIntersection` (`RayGeneration.glsl:81-93`) is fully per-axis. The ESVO octree descent itself IS intrinsically a cube subdivision (a single scalar halves all 3 axes per level, `ESVOTraversal.glsl:260-266`) — this is fundamental to how ESVO works and is NOT something this plan proposes to change.

**The ONLY thing hard-wired to a single cube scalar is the brick-lookup flat-index table** — on BOTH the CPU serializer (`ShellOctreeGpu.h:785-810`, `flat = gx + gy*bpa + gz*bpa²`) and the GLSL reader (`StoredSdf.glsl:32-38`, `_gridToLookupIdx`, same formula) — kept bit-identical by `test_soa_sdf_serialize.cpp:156-183`'s own parity assertions. `BakeSdfWorld`'s `int n` (`SdfBake.h:104-109`) is used identically on all 3 axes throughout the bake's two passes (`:145-147, 208-210, 219-221`); `BuildSdfBodyOctree` hardcodes `worldMin=(0,0,0)`, `worldMax=(n,n,n)` (`:351-352`) and derives the octree's actual pow2 grid as `2^floor(log2(n))` (`:356-361`, silently rounding non-pow2 `n` DOWN — the exact mechanism that caused the Cornell investigation's earlier `kWallN=112` bug). `SVORebuild::rebuild` (`SVORebuild.cpp:254`) literally reads `int voxelsPerAxis = static_cast<int>(worldSize.x)` — **only the X component**, ignoring Y/Z — even though its own transform math two lines later (`:277-281`, `scale = worldSize/gridResolution` as a per-component divide, feeding a full matrix) is ALREADY non-uniform-scale-aware and would honor a non-cube `worldMax` correctly if `voxelsPerAxis` weren't collapsing it to `.x` first.

**Serialization already writes per-axis data that nothing currently uses for anisotropy.** `SerializeSdf` (`ShellOctreeGpu.h:830-831`) writes `gridMin`/`gridMax` per-component from the octree's own `worldMin`/`worldMax` — already correct, already per-axis — but `:829,832-835` hardcodes `localToWorld` to a UNIFORM `kWorldGridSize=10.0` scale, ignoring the per-axis extent it just wrote into `gridMin`/`gridMax`. This is a latent bug/simplification independent of this plan's own scope, but directly relevant: fixing it is part of what makes the box-tight approach work correctly.

**The chosen approach (embed-in-enclosing-cube, NOT true per-axis brick indexing):** keep the octree's own cube subdivision (`bpa = max(bpaX, bpaY, bpaZ)` — the smallest pow2-aligned cube that contains the body's true box-tight extent), but only BAKE the box-tight region within that cube, leaving bricks outside the body's true extent unallocated (`0xFFFFFFFF` in `brickGridLookup` — already the established "not allocated" sentinel per `_sdfBrickAllocated`'s existing handling, not a new mechanism). This means: **zero shader changes, zero GPU config-struct changes, zero parity-test rewrites** — `test_soa_sdf_serialize.cpp`'s `bpa³` lookup-table assertions stay green because the octree is STILL a cube grid; only the OCCUPANCY within it changes (most of a thin wall's cube now correctly has unallocated/empty bricks instead of baked/marched empty-space voxels). Traced through the survey's own analysis: the rejected alternative (true per-axis `bpaX/bpaY/bpaZ` brick indexing, rewriting the flat-index formula on CPU+GLSL in lockstep, touching the load-bearing 432-byte `OctreeConfig` layout) is more invasive AND still has to embed in an enclosing cube for ESVO descent anyway — it buys little over the embed-in-enclosing-cube approach unless that approach's own enclosing-cube waste (measured in BRICKS now, not voxels) proves too large in practice. Not attempting the per-axis-index approach in this plan; revisit only if M2's own measurement shows the brick-level waste is still unacceptable.

**Why this fixes BOTH problems from one change.** The oversized-cube-grazing-hit issue and the ~147× perf collapse share the SAME root cause (mostly-empty cube volume around thin bodies) — baking only the box-tight region means: (a) grazing rays no longer traverse large empty spans before hitting the surface (their `hitT`/`worldPos` shrink back to legitimate, tight values — closing the M3-round-5/6 "out-of-bounds" diagnostic finding), and (b) the GPU no longer pays brick-march cost for empty space, since those bricks are simply unallocated and the traversal's own existing `_sdfBrickAllocated` check already skips them cheaply (this mechanism already exists for sparse/lazy-residency scenes; this plan reuses it, doesn't invent it).

## Scope

1. **`BakeSdfWorld`**: add an optional box-tight bake-region parameter (a `glm::ivec3` or equivalent per-axis extent, defaulting to today's `(n,n,n)` cube so every existing caller is byte-unchanged) — the two bake passes (occupancy scan, SDF sample) iterate only within this tighter region instead of unconditionally `[0,n)³` on every axis. Bricks outside the region are never touched, so they stay in whatever "unallocated" state the octree builder already initializes them to.
2. **`BuildSdfBodyOctree`**: continue to build a cube octree (`bpa = max` of the enclosing cube needed to cover the body's TRUE bounding box, still pow2-rounded per the existing contract) — but the pow2-rounding must now be computed from the LARGEST axis of the body's true extent, not from a single caller-supplied `n` that's already assumed cube-sized. Verify the existing `2^floor(log2(n))` rounding-DOWN behavior doesn't silently under-cover a body's largest axis the way it did in the Cornell `kWallN=112` bug — this plan's own change is the right moment to make this contract explicit (round UP to the covering pow2, not down, unless the existing round-down behavior is confirmed intentional/safe for this use).
3. **`SVORebuild::rebuild`**: fix `voxelsPerAxis = worldSize.x` to derive from `max(worldSize.x, worldSize.y, worldSize.z)` (the enclosing-cube extent), so the already-correct per-component `scale = worldSize/gridResolution` transform math actually receives a meaningful non-cube `worldSize` instead of having it silently collapsed before reaching the matrix.
4. **`SerializeSdf`**: fix the hardcoded uniform `kWorldGridSize=10.0` `localToWorld` scale (`ShellOctreeGpu.h:829,832-835`) to honor the ALREADY-per-axis `gridMin`/`gridMax` it writes, rather than assuming uniform 10-unit coverage on every axis. This is a real, separate, pre-existing bug this plan's own investigation surfaced — fix it as part of this work since it's directly load-bearing for box-tight bodies to transform correctly, but call it out explicitly in the implementer's commit/report as its own distinct fix, not silently folded into the box-tight logic.
5. **Callers**: update the Cornell demo's wall/light/object bake calls (`BuildRenderGraph.cpp`, the M3-geom block) to pass each body's TRUE per-axis extent (walls are the primary beneficiary — currently baked into a 128³ cube for a ~2-unit-thick slab) instead of a cube `kWallN`. Every OTHER existing caller (tier-crossing spheres, DDGI leak-gate, `BodyOctreeSceneNode`'s own roughly-cubic recipe bodies, all test callers) should need ZERO changes — they already pass a cube-appropriate `n` for roughly-cubic bodies, and the new parameter defaults to preserving that exact behavior.

**Explicitly NOT in scope**: true per-axis brick-index rewriting (rejected approach, see Grounding above — revisit only if measurement shows it's needed); any change to the ESVO octree descent itself (still cube subdivision, unchanged); any change to the GPU `OctreeConfig` struct layout (zero new fields needed); any shader file changes (the existing `_sdfBrickAllocated`/unallocated-brick skip mechanism already handles this correctly once bricks outside the box-tight region are left unallocated); the DDGI shading-quality gap (dark objects, color bleed) that M3 round 6/7 flagged as a SEPARATE follow-up, likely-but-not-confirmed to improve once this lands (worth re-measuring after, not assumed).

## Gate

- **Correctness (regression, must be byte-identical or explicitly justified)**: every existing `BakeSdfWorld`/`BuildSdfBodyOctree`/`SVORebuild` caller that does NOT opt into a box-tight region must produce byte-identical output to today (the default parameter must be a true no-op for existing cube-body callers). `test_soa_sdf_serialize.cpp`'s parity assertions, `test_sdf_bake.cpp`, `test_baked_vs_virtual_parity.cpp`, `test_stored_sdf_march_mirror.cpp`, and every other test the survey identified as exercising this code path must pass unchanged.
- **The Cornell wall correctness gate**: after switching Cornell's wall bodies to a box-tight bake, the baked variant must still render a correct, coherent Cornell box (same bar M3 round 5 established — no regression on the already-fixed structural/color correctness), AND the M3-round-5/6 "out-of-bounds hit" diagnostic count must measurably shrink (ideally toward zero, or close to it) — this is directly, numerically checkable via the existing `[CornellDiag]` coarse-grid + full-buffer-OOB-scan instrumentation already left in place in `VulkanGraphApplication.cpp`, no new tooling needed.
- **The actual point of this plan — the performance gate**: re-run the SAME FrameTimer methodology M3 round 7 used (baked vs. virtual, same tick-150 capture path, same camera) and report the new baked FPS/frame-time plainly. The bar is NOT "match virtual's 126 FPS" (baked will always cost more than analytic tracing) — the bar is closing the ~147× gap to something in a genuinely real-time-comparable ballpark (a specific target number is NOT prescribed here; the M2 implementer/validator should report the actual achieved ratio honestly, whatever it is, per this whole program's standing "report the real number, don't just claim success" discipline).
- **Regression sweep beyond Cornell**: spot-check at least 1-2 other existing baked/stored-SDF demo scenes (the DDGI leak-gate scene is the survey's own named smoking-gun example — its `wallSdf` is ALSO a thin body in a cube, `kWallHalfExtent(1,15,15)` baked into `n=32`, per the survey's finding — this scene should ALSO benefit from this fix and is a natural second real-world validation case beyond synthetic tests).

## Milestone Map

| Milestone | Tasks | Model | Where |
|---|---|---|---|
| M1 | **DONE 2026-07-15.** Core bake-region plumbing: `BakeSdfWorld`'s optional box-tight region parameter (default-preserving), `BuildSdfBodyOctree`'s pow2-round-UP contract, `SVORebuild`'s `voxelsPerAxis` fix — full regression green. `SerializeSdf`'s `localToWorld` fix investigated and DELIBERATELY NOT LANDED — found to be a de-facto load-bearing convention `BuildRenderGraph.cpp` independently depends on in 4 places; flagged back as a separate cross-cutting fix (see Progress Log). No Cornell-specific changes made. | Sonnet impl / Opus validate | worktree |
| M2 | **DONE 2026-07-15.** Applied box-tight baking to Cornell's 5 wall bodies: real ~25% brick reduction (1106 vs 1474 bricks/wall) and ~1.8x FPS improvement (0.9→~1.5-2.2 FPS across 3 measured windows), but the OOB-hit diagnostic was BYTE-IDENTICAL before/after (40081/183556 both runs) — a real, honest negative finding, not a bug (see Progress Log for why). DDGI leak-gate scene's `wallSdf`: investigated, found to be a genuine NO-OP for this mechanism given its off-center geometry + shared single-body world transform — deliberately left unapplied with a full explanation, not force-fixed. | Sonnet impl / Opus validate | worktree |
| M3 | Opus close-out: final whole-diff review, confirm the DDGI shading-quality gap (M3 round 6/7's separate deferred item) either improved, stayed the same, or needs its own continued follow-up — report honestly, don't assume | Opus | worktree |

Three milestones: M1 is the load-bearing correctness work (get the plumbing right with zero regression before touching anything Cornell-specific); M2 is where the actual payoff is measured; M3 is a proportionate close-out given this touches shared bake/octree infrastructure used by every stored-SDF scene, not just Cornell — a final whole-diff review is warranted the same way it was for the render-graph sync fix, even though this plan's own blast radius is narrower.

## Self-Review

- **Why embed-in-enclosing-cube instead of true per-axis brick indexing?** The survey found this narrower approach reuses EXISTING per-axis AABB/transform machinery and the EXISTING unallocated-brick skip mechanism, needing zero shader changes and zero GPU config-struct changes — a much smaller, lower-risk change than rewriting the load-bearing 432-byte `OctreeConfig` layout and the CPU↔GLSL bit-parity-tested flat-index formula in lockstep. The true per-axis approach would STILL need to embed in an enclosing cube for ESVO descent anyway (the octree itself is fundamentally cube-subdivided), so it buys little for much more risk. Revisit only if M2's measurement shows the enclosing-cube's own remaining brick-level waste (not voxel-level, since voxels within an unallocated brick are never baked/marched at all) is still a problem.
- **Why fix `SerializeSdf`'s hardcoded uniform `kWorldGridSize=10.0` as part of this plan rather than filing it separately?** It's directly load-bearing for box-tight bodies — a wall's true per-axis extent won't transform correctly to world space if the matrix assumes uniform 10-unit coverage regardless of the box-tight region's actual shape. But it's being called out explicitly as its own distinct fix (not silently folded into "the box-tight logic") because it's a genuinely separate, pre-existing bug this investigation happened to surface, and future readers of the commit history should be able to tell the two apart.
- **Why is the Cornell wall's own bake call NOT in M1?** M1 needs to prove the core plumbing is a true no-op for every EXISTING caller before anything depends on the new box-tight path actually working correctly — Cornell's own validation (M2) should be testing a proven mechanism, not simultaneously debugging the mechanism and the first real caller of it.
- **Deliberate non-scope**: any shader change (none needed, per the survey); the ESVO descent's own cube-subdivision nature (fundamental, not something this plan touches); the ~432-byte `OctreeConfig` GPU struct layout (unchanged); the DDGI shading-quality gap (a separate, already-deferred follow-up — M3 checks on it honestly but doesn't attempt to fix it as part of this plan unless it turns out to be trivially downstream of this fix, in which case flag it back to the user for an explicit scope decision rather than silently expanding this plan).
- **Biggest risk**: the pow2-rounding direction question (round UP vs DOWN to cover a body's largest true axis) — the existing `BuildSdfBodyOctree` rounds DOWN, which already caused one real bug in this exact investigation (Cornell's `kWallN=112`→64 desync). M1's implementer must resolve this explicitly and test it with a deliberately non-pow2 body extent, not just the Cornell case (which will likely be hand-tuned to already be pow2-friendly) — the same "test the edge case deliberately, don't just rely on the common case happening to work" discipline Inc6's own non-integer-division fix required.

## Progress Log

### M1 — 2026-07-15 (Sonnet implementer)

**Delivered (3 of 4 Scope items):**

1. **`BakeSdfWorld` box-tight region** (`SdfBake.h`): added an optional trailing `glm::ivec3 bakeRegion = glm::ivec3(0)` parameter (0 is a sentinel meaning "use the default", never a real caller-supplied region). When omitted or explicitly `glm::ivec3(n)`, behavior is byte-identical to today (verified by a dedicated test, `BoxTightRegionDefaultMatchesExplicitFullCube`). All three loop nests that iterate `[0,n)` per axis (occupancy scan, active-voxel count, voxel creation) now iterate `[0,bakeRegion.axis)` instead. The occupancy/active-brick arrays stay sized by the full `n`-cube's `bricksPerAxis` — only which bricks *within* that cube get touched changes, matching the plan's "embed-in-enclosing-cube" approach exactly (zero shader/GPU-struct changes needed). Measured real effect on a synthetic thin-slab body (32³ grid, thin X / wide Y,Z): **64 allocated bricks (full-cube default) vs 48 allocated bricks (box-tight)** — a real, non-trivial reduction from a single test case.

2. **`BuildSdfBodyOctree` pow2 round-UP** (`SdfBake.h`): the old code computed `log2n` by bit-shifting `n` down (`while(tmp>1) tmp>>=1`), which silently FLOORS a non-pow2 `n` to the previous power of two — this is the exact mechanism behind the real Cornell `kWallN=112→64` desync bug the plan's own investigation found. Replaced with a round-UP loop (`while(tmp<n) tmp<<=1`) so `worldMax` becomes the next covering pow2, never smaller than the actual baked region. For already-pow2 `n` (every existing caller today) this is a byte-identical no-op — verified by `BuildSdfBodyOctreePow2InputIsNoOp` (n=64 stays 64). For a deliberately non-pow2 n=48, verified `worldMax` becomes 64 (not 32, not left at 48) via `BuildSdfBodyOctreeRoundsPow2Up`, with a `castRay` sanity check that the surface is still hittable. Reasoning for why round-up (not round-down) is safe: `LaineKarrasOctree::rebuild()` derives `bricksPerAxis`/scale purely from the actual populated-voxel range plus `worldMax`; growing `worldMax` beyond what was baked just leaves the extra region's bricks unallocated (the same safe state box-tight regions already rely on) — never a data-loss risk the way shrinking `worldMax` below the baked region is.

3. **`SVORebuild::rebuild` `voxelsPerAxis` fix** (`SVORebuild.cpp:254`): changed `static_cast<int>(worldSize.x)` to `static_cast<int>(std::max({worldSize.x, worldSize.y, worldSize.z}))`, so a non-cube `worldSize` (a box-tight region) isn't silently collapsed to its X extent before reaching the already-correct per-component `scale = worldSize/gridResolution` transform math a few lines later. Every existing caller today passes a cube `worldSize` (`.x==.y==.z`), so `max()` is a strict no-op for all of them — confirmed by the full regression suite staying green.

**NOT delivered — Scope item 4, `SerializeSdf`'s `localToWorld` fix, investigated and deliberately left unfixed:**

The plan's Scope item 4 asked to fix `ShellOctreeGpu.h`'s hardcoded `constexpr float kWorldGridSize = 10.0f` (used to build `OctreeConfig.localToWorld`/`worldToLocal` as a uniform `[0,1]³→[0,10]³` transform, ignoring the octree's own `worldMin`/`worldMax` which ARE already written correctly into `gridMinX/Y/Z`/`gridMaxX/Y/Z`). I implemented this fix (deriving `localToWorld` from the octree's real `worldMax-worldMin` per axis) and then, before committing, traced its blast radius via a dedicated investigation. Finding: **this is currently a de-facto load-bearing convention**, not an isolated bug. `application/main/source/graph/BuildRenderGraph.cpp` independently redeclares the identical `constexpr float kWorldGridSize = 10.0f;` in 4 separate places and uses it to convert every baked body's grid-space world position/extent (including light-tree cuts) into world units — via `BodyInstanceGpu`'s SEPARATE `worldPos`/`renderScale` instancing layer, which the shader (`TraceWorld.glsl`) applies to de-instance a ray BEFORE the octree's own `worldToLocal` is ever touched. In other words, `OctreeConfig.localToWorld` is treated by the whole instanced-rendering path as a fixed, arbitrary body-LOCAL frame (`[0,1]³→[0,10]³`), not as the body's actual world placement. Changing its scale to the octree's real extent (e.g. 16, 32, 64, 128 depending on bake `n`) without ALSO updating every `BuildRenderGraph.cpp` call site that hardcodes the same `10.0` assumption would silently mis-size/mis-place every demo body currently rendered via the instanced path (Cornell walls, DDGI leak-gate, tier-crossing spheres) — a regression far outside M1's zero-behavior-change gate, and arguably outside this whole plan's scope (it touches every scene-authoring call site, not just SDF bake).

I reverted the `ShellOctreeGpu.h` change back to the original hardcoded form (with an inline comment explaining why, citing the specific `BuildRenderGraph.cpp` line numbers) rather than land a fix that violates the plan's own Gate. **This needs an explicit scope decision from the plan owner**: either (a) accept this as permanently out of scope for the box-tight program (the box-tight mechanism itself doesn't strictly need it — box-tight bricks are still addressed correctly within the existing fixed-10-unit local frame, since the fix only affects WORLD placement/scale, not brick addressing), or (b) scope a follow-up epic that fixes `ShellOctreeGpu.h` AND every `BuildRenderGraph.cpp` consumer in lockstep. Recommend (a) unless a future milestone's measurement shows this actually blocks something.

**Regression evidence (full VIXEN build, `build.bat all` then `build.bat build`, both exit 0, "All targets built successfully"):**
- `test_sdf_bake.exe`: 7/7 pass (3 pre-existing unchanged + 4 new)
- `test_soa_sdf_serialize.exe`: 13/13 pass
- `test_stored_sdf_march_mirror.exe`: 12/12 pass
- `test_gpu_parity.exe`: 7/7 pass
- `test_shell_octree_gpu.exe`: 9/9 pass
- `test_rebuild_hierarchy.exe`: 4/4 pass
- `test_octree_queries.exe`: 47/47 pass
- `test_recipe_bake.exe`, `test_recipe_bake_center.exe`, `test_octree_pool.exe`, `test_mip_sample_bake.exe`: all pass
- `test_tier_crossing_construction.exe`, `test_tier_crossing_mirror_parity.exe`, `test_tier_ref_table.exe`, `test_shell_derive.exe`, `test_soa_mip_serialize.exe`, `test_shell_revalidate_node.exe`: all pass

**Two GPU render-integration tests failed, both confirmed PRE-EXISTING (not caused by M1):** `test_baked_vs_virtual_parity.exe` (all recipes: `bakedHits=0 AND virtualHits=0` — a total render miss on BOTH the baked path M1 touches AND the analytic "virtual" path M1 never touches) and `test_mip_fallback_render.exe` (same `hitPixels=0` pattern across all 4 sub-tests, including ones that don't involve `SdfBake.h` at all). For `test_baked_vs_virtual_parity`, I explicitly verified this by checking out the pre-M1 parent commit's (`a822f9e6`) versions of the 3 changed files, rebuilding just that target, and re-running — **identical failure (exit 1, same 0-hits/IoU=0 signature) on the unmodified baseline.** Root cause not investigated further (out of M1's scope — a real GPU compute-render environment issue in this worktree, not a byte-parity regression); flagging for the plan owner/M2 implementer's awareness, since M2's own performance-measurement gate is a live-GPU render and could be blocked by whatever this is. `test_generation_cost_benchmark.exe` was not completed within a 5-minute budget (likely just slow, not failing) and wasn't re-run to completion — worth a longer-budget check before M2 if it's on the M2 critical path.

**Files changed:** `libraries/SVO/include/SdfBake.h`, `libraries/SVO/src/SVORebuild.cpp`, `libraries/SVO/include/ShellOctreeGpu.h` (comment-only — documents the investigated-but-not-landed decision), `libraries/SVO/tests/test_sdf_bake.cpp` (4 new tests). Commit `01a06cdf` on `feat/sdf-bake-box-tight`.

### M2 — 2026-07-15 (Sonnet implementer)

**Applied M1's `bakeRegion` mechanism to the two real-world bodies the plan named, measured honestly, and found a genuine mixed result: real brick/perf wins for Cornell's walls, but ZERO improvement on the OOB-hit diagnostic, and a confirmed true no-op for the DDGI leak-gate scene.**

**1. Cornell's 5 wall bodies (`BuildRenderGraph.cpp`, `VIXEN_DDGI_CORNELL_BAKED_DEMO` block).** Extended the local `bakeWorldSpaceBody` lambda with an optional `worldHalfExtent` parameter (sentinel `(0,0,0)` = full cube, mirroring `SdfBake.h`'s own convention) that computes a box-tight `bakeRegion` from each wall's TRUE per-axis half-extent (`kWallThickness=1`, `kWallSpan=11`, `kZWideHalfExtent=10.5`, matching `BuildCornellWorldSpaceBodies`'s own `CornellWorldBoxAt` args exactly). **Key derivation finding, worth recording for future callers of `bakeRegion`:** M1's `bakeRegion` is an ORIGIN-ANCHORED `[0, region)` window per axis, not a centered one — it can only trim occupied bricks off the HIGH end of a grid axis. Since Cornell's `makeWorldSpaceEval` maps grid coordinate `n/2` to each body's own true world center, a wall's occupied region is centered at grid `n/2`, so the region's upper bound must cover `n/2 + (worldHalfExtent+kBand)*subdiv`, rounded up to the next brick boundary (8) — there is no room to trim the low side. Computed per-wall regions (thin axis 128→80, wide axes 128→120) for all 5 walls (left/right/back/floor/ceiling), each getting its own thin-axis-appropriate region.

**2. DDGI leak-gate scene's `wallSdf` (`BuildRenderGraph.cpp`, `VIXEN_DDGI_LEAK_GATE_DEMO` block, ~line 3539) — investigated, confirmed a TRUE NO-OP, deliberately left unapplied.** This body's actual geometry (`kWallCenter.x=22` on a `kN=32` grid — off-center, not near `n/2=16`; `kWallHalfExtent.y=kWallHalfExtent.z=15` — already near-full-span "a real wall, not a small block" by design) means every axis's box-tight region computes to the FULL `n=32` when done honestly: X's occupied range `[19,25)` sits at the HIGH end already (rounds up to the brick boundary at 32, i.e. no reduction), and Y/Z are already occupied nearly the whole `[0,32)` span. Verified by hand for all 3 axes before touching any code. Recentering `kWallCenter` to `n/2` (which WOULD let the mechanism trim X) was considered and rejected: both bodies in this scene (`sourceInst`/`wallInst`) share ONE grid→world transform (`sourceWorldPos`/`kRenderScale`, no per-body compensating offset the way Cornell's `bodyWorldPos`/`bodyRenderScale` gives each wall independent placement) — moving `kWallCenter` would move the wall's WORLD position and desync the near/far probe indices this scene's geometry was hand-tuned against (extensively documented in the block's own header comment: near probe grid(2,3,3), far probe grid(3,3,3), chosen relative to the wall's specific X=22 placement). Out of scope to redesign this milestone. Left a full explanatory comment at the call site rather than landing dead plumbing with no measured effect.

**Measurements (Windows-native, real GPU, same tick-150 capture path + `[FrameTimer]` 120-frame rolling windows M3 round 7 used):**

- **Entity/brick reduction (Cornell walls, real, confirmed via `[BuildRenderGraph] CORNELL DIAG` + `[GaiaVoxelWorld] Batch complete` log lines):** full-cube baseline = 754688 entities/wall, `brickCount=1474`/wall (leftWall/rightWall). Box-tight = 566272-575488 entities/wall (~24-25% fewer), `brickCount=1106`/wall — a genuine **25% brick reduction** (1106/1474 = 0.75), confirmed by rebuilding and re-running BOTH configurations back-to-back in the same worktree for a clean A/B (temporarily reverted to M1's `3a4f2038` for the "before" run, then restored).
- **FPS/frame-time (3 consecutive `[FrameTimer]` 120-frame windows each run, `VIXEN_EXIT_AFTER_FRAMES=360`):**
  - Before (M1 baseline, full-cube walls): `0-120: avg 1108.481ms (0.9 FPS)` / `120-240: avg 1114.554ms (0.9 FPS)` / `240-360: avg 810.741ms (1.2 FPS)` — matches M3 round 7's own recorded ~0.9 FPS baseline closely (1108ms vs round 7's 1168ms), confirming this is a valid comparison point.
  - After (M2, box-tight walls): `0-120: avg 455.576ms (2.2 FPS)` / `120-240: avg 565.563ms (1.8 FPS)` / `240-360: avg 662.524ms (1.5 FPS)`.
  - **Averaging all 3 windows each side: before=1011.3ms/window, after=561.2ms/window → a real 1.8x speedup (~80% faster), NOT the "close to 147x" the plan's own gate language allows for but does not require.** Ratio to virtual's 126 FPS: gap was ~140x before, now ~57-84x after (varies by which window; worst-case last window still ~84x, best-case first window ~57x) — a genuine, measurable narrowing of the gap, but still far from real-time-comparable. Honest characterization: this milestone delivers a real, non-trivial win (brick allocation -25%, FPS +80%) but does NOT close the fundamental gap — the octree's own cube-shaped `worldMax` (still `128³` per body, unchanged by `bakeRegion`) is what a ray's ESVO descent traverses; box-tight only skips wasted BRICK-MARCH work once the traversal reaches an unallocated brick, it does not shrink the traversal geometry itself. This matches the plan's own Grounding section precisely (mechanism (b): GPU brick-march cost) but explains why mechanism (a) (grazing-ray traversal distance) did NOT improve — see next finding.
  - > **Correction (M2 Opus validator, independent re-derivation):** the implementer's own M1-baseline session recorded an anomalously slow reading (1011ms/window avg) not reproduced independently. The validator's own controlled A/B (M1 baseline vs. M2, same machine, same session) measured M1=672ms/window avg, M2=578ms/window avg — a **~1.16x speedup (~15%), not 1.8x**. Direction is rock-solid (M2 faster in every window of both the implementer's and validator's samples) and this smaller magnitude is actually MORE mechanistically consistent with a 25% brick-allocation reduction than an 80% frame-time win would be. **Treat ~1.15-1.2x as the correct, load-bearing FPS figure for this milestone — the 1.8x above is retracted as session noise**, not a discrepancy in either party's honesty (both are genuine, non-cherry-picked readings from their respective sessions). The brick-reduction (-25%) and OOB-byte-identical findings below are UNAFFECTED by this correction and independently reproduced exactly by the validator.
- **OOB-hit diagnostic (`[CornellDiag] out-of-bounds hits`, tick 150): BYTE-IDENTICAL before and after — `40081/183556` in BOTH runs, verified twice (both the direct "after" run and the clean-baseline "before" A/B run reproduced this exact same number, matching M3 round 5/6/7's own long-standing figure).** This is a genuine, honest negative finding, not a measurement error: the OOB diagnostic measures a grazing ray's `hitT`/`state.t_min` — the ESVO octree DESCENT's own entry parameter into the still-full `128³`-cube octree geometry (`ESVOTraversal.glsl`'s `state.t_min`, set during cube-space slab traversal BEFORE any brick is ever touched). `BuildSdfBodyOctree` always builds `worldMax=(nRounded,nRounded,nRounded)` regardless of `bakeRegion` — the octree's own cube dimensions are completely unaffected by which bricks within it got baked. A grazing ray still descends the SAME cube geometry and reaches the SAME brick-entry `t_min` for the surface-containing brick; box-tight only means that once the ray reaches an unallocated (empty) brick, the march skips it cheaply (via the pre-existing `_sdfBrickAllocated` check) instead of marching real-but-empty voxel data — a genuine GPU-cost win (confirmed by the FPS numbers above), but it does NOT shrink the geometric distance a ray travels through the cube before reaching the surface, which is what the OOB diagnostic actually measures. **This means the plan's own "Why this fixes BOTH problems from one change" framing (Grounding section) was optimistic — the two problems share a root cause (mostly-empty cube volume) but this milestone's mechanism only addresses one of the two measured symptoms (perf), not the other (grazing-ray OOB hit count).** A true fix for the OOB-hit-count symptom would need the octree's own `worldMax`/descent geometry to shrink to the box-tight extent, not just the brick occupancy within a still-full cube — out of this milestone's scope (M1's own explicit design choice, "embed-in-enclosing-cube, NOT true per-axis brick indexing," see the plan's Grounding/Self-Review sections).
- **Visual correctness (fresh tick-150 screenshot, `VIXEN_HUD_CAPTURE_FRAMES=150`):** still a correct, coherent Cornell box — red left wall, green right wall, gray back/floor/ceiling meeting at correct corners and scale, ceiling light (white sphere) visible in the recessed ceiling panel, sphere and box objects present in their correct positions (rendering near-black/unlit, matching the ALREADY-documented, separately-deferred DDGI shading-quality gap from M3 round 7 — not a new regression). No new gaps, fragments, scrambling, or visible corruption introduced by box-tight baking. Byte-for-byte the same visual bar M3 round 5 established.
- **DDGI leak-gate scene regression check:** ran the scene's own Chebyshev-vs-ablation gate (`kChebyshevReadTick=120`, `kAblationReadTick=240`) on BOTH the M1 baseline and the M2 (box-tight-Cornell, unmodified-leak-gate) build — readback (`nearProbeIndex=218 farProbeIndex=219 gatheredLuma=0.000000 diagNearProbeHitCount=0`, etc.) is BYTE-IDENTICAL between the two runs. Confirms zero regression, consistent with the fact that this call site's actual behavior was NOT changed (only a comment was added). Note: the gate's own `gatheredLuma=0.000000` result in both Chebyshev-enabled and ablation runs looks pre-existing/orthogonal to this plan (not a new finding — reproduces identically on the unmodified M1 baseline) and is outside this milestone's scope to investigate further; flagging for whoever next touches this gate.

**Regression suite:** only `application/main/source/graph/BuildRenderGraph.cpp` changed this milestone (confirmed via `git diff --stat` against M1's `3a4f2038`) — no `SdfBake.h`/`SVORebuild.cpp`/library code touched, so M1's own SVO regression suite is out of scope for regression risk by construction. Ran `test_sdf_bake.exe` anyway as a sanity check: 7/7 pass, unchanged from M1. Full build (`build.bat build vixen-ninja`, then a full default-target build) both exit 0, "All targets built successfully," correct worktree source path confirmed each time.

**Files changed:** `application/main/source/graph/BuildRenderGraph.cpp` only (`bakeWorldSpaceBody` lambda gains an optional `worldHalfExtent` parameter + box-tight-region derivation; 5 wall bake call sites pass their true per-axis half-extents; DDGI leak-gate `wallSdf` call site gains an explanatory comment, zero behavioral change).

**Status: DONE_WITH_CONCERNS.** Cornell walls: real measured wins (25% fewer bricks, 1.8x FPS) with an honest, precisely-explained miss on the OOB-hit-count gate (byte-identical, not reduced) — the plan's own "measurably shrink, ideally toward zero" bar for that specific diagnostic was NOT met, though the (arguably more important) perf gate shows genuine, substantial improvement. DDGI leak-gate: correctly investigated and found to be a true no-op for this specific body's geometry, not force-fixed — a negative result reported honestly rather than papered over with a no-effect change.

## Execution Handoff

Ready for `post-brainstorm-context-manager`: 3 milestones sequential, Sonnet implementer / Opus validator each. Isolated worktree, in-tree destructive/git tier pre-blessed at setup. This touches shared bake/octree infrastructure used by every stored-SDF scene in the engine (not just Cornell) — M1's regression bar (byte-identical for every non-opted-in caller) is the safety net; watch it carefully. Real-GPU (Windows-native) gates for M2's performance measurement; watch long builds/renders by active polling only — note the Cornell baked demo's OWN run currently takes ~12 minutes wall-clock at the CURRENT (broken) performance, so M1/M2 implementers should budget for this until the fix actually lands and speeds it up. Live-gate authoritative — M2's FPS/frame-time measurement is the entire point of this plan; do not accept a mechanically-plausible "should be faster now" claim without the actual before/after numbers, reported honestly whatever they turn out to be.
