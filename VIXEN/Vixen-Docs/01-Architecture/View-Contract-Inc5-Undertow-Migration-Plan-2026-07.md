# View Contract Inc-5 — Undertow Migration Plan (2026-07-12)

**Program:** `Renderer-Agnostic-View-Contract-Design-2026-07.md` §6.5 (the undertow system this program
subsumes), §3 (four faces). **Prior:** Inc-1 (data emitter) + Inc-2/2b (renderer-agnostic `IView` host +
reflection blob/generic marshaler) + Inc-3 (C# data-upload face, AoS wire, round-trip proven) — **ALL DONE
and merged to both origins** (VIXEN main, Yeroket main). Inc-3's own doc named this deferred slice
"Inc-5+" explicitly (D1: "the undertow migration... is Inc-5+").

## Goal

Migrate undertow's hand-rolled `Undertow.Authoring.Codegen`/`ViewSchema.cs` view-contract codegen onto the
shared Yeroket kernel-framework `[View]` schema + emitter family, so undertow becomes a **consumer** of the
content format instead of its **author** — reversing the current, documented-as-temporary inversion (see
Ground Truth below) and completing `[[vixen-owns-content-format-not-consumer]]` for the one remaining
system that still violates it.

## Ground truth this plan is scoped against (verified by direct research 2026-07-12 — do not re-derive,
but DO re-verify file:line if code has moved)

- **Undertow's codegen is real, live, load-bearing — not a stub.** `core/src/Undertow.Authoring.Codegen/`
  is a genuine Roslyn `IIncrementalGenerator`, wired as an MSBuild `Analyzer`-type `ProjectReference`
  (`Undertow.View/Undertow.View.csproj:11-12`) — it runs at compile time, not via a manual script. ~35
  emitter files (`Emit.cs`, `EmitCodec.cs`, `EmitPatchParser.cs`, `EmitMerge.cs`, `EmitViewContractHeader.cs`,
  `EmitViewWriter.cs`/`ViewWriterGenerator.cs`, etc.) — **this covers far more than view codegen alone**
  (authored-kind builders, save codecs, patch parsers, effect/action/param/system registration, merge
  logic). **Scope discipline: this plan is ONLY the view-contract slice** (`ViewSchema.cs` +
  `EmitViewContractHeader.cs` + `EmitViewWriter.cs`/`ViewWriterGenerator.cs`) — the other ~30 emitter
  files (save codecs, patch parsers, authored-kind builders) are explicitly OUT of scope; do not let this
  plan creep into "migrate all of undertow's codegen," which is a much larger, separate epic.
- **`ViewSchema.cs`** (`core/src/Undertow.View/ViewSchema.cs`, read in full 2026-07-12) — 5 sections
  (`Bodies`, `Hud`, `HudFactions`, `HudEvents`, `HudInspect`), hand-bumped `FormatVersion = 10`, a 9-entry
  intro-version ladder (`VersionHud=2` … `VersionRecipe=10`), 8 column types
  (`F32/F64/I32/U32/U8/Vec3f/ListVec3f/Str`), magic bytes `UTVW` (view)/`UTFB` (feedback). Every generated
  file carries its own `<auto-generated/> DO NOT EDIT` banner (`GeneratedHeader.cs:19-21`) pointing at
  `ViewSchema.cs` as source of truth, guarded by golden tests
  (`ViewContractHeaderTests.cs`/`SchemaJsonEmitTests.cs`/`GeneratedHeaderTests.cs`/`SchemaDisclaimerTests.cs`).
- **Zero Yeroket kernel-framework consumption exists in undertow today.** No `PackageReference`, no
  submodule, no copy-pasted source. The one hit (`unity/Packages/com.undertow.app/Runtime/KernelJobs/
  TagDensityKernel.cs:8`) is an aspirational COMMENT ("In the real implementation this becomes a
  `[VMKernel]`..."), not a real wiring — the job is hand-written Burst code today.
- **Two unrelated "N2"s exist in undertow's own history** (a naming collision worth knowing so this plan's
  own future increments don't reuse "N2" a third time): (a) `1d53a4c6` — kernel-unification program's
  cleanup deleting a stale vendored `OctreeConfig.g.h`, confirmed ON `master`'s ancestry (an earlier memory
  note calling this "unpushed" was stale/wrong, corrected 2026-07-12); (b) `9094d30e`/`35b8f0d8` — an
  unrelated, docs-only, apparently-abandoned "nested-shape codegen N2 / ConstraintNode" spec from
  2026-06-04, no corresponding code exists. Neither is this plan's concern; noted only to prevent
  confusion if either surfaces during research.
- **§6.5's "subsumed by Inc-3" was never actually true and is NOT what Inc-3 claimed** — Inc-3's own D1
  explicitly excludes undertow ("Do NOT modify undertow... is Inc-5+"). The current undertow tree is
  verified UNCHANGED and still exactly matches the pre-program state. This plan is the first increment
  that actually touches undertow.
- **The real missing prerequisite: SoA wire emit + reader.** Inc-3 shipped the AoS (`UTVA`) wire only —
  its own M2 test proves `ViewLayout.Soa` currently THROWS a clear `NotSupportedException("SoA emit is
  Inc-5+...")` by design (Inc-3 Plan doc, Milestone M2). Undertow's actual wire is columnar/SoA (per
  `ViewSchema.cs`'s own doc comment: "a columnar (SoA) binary `SimFrame` slice" — memory:
  `undertow-vixen-integration-map.md`). **This plan cannot migrate undertow's actual wire format until
  SoA emit + a SoA-aware C++ reader exist** — this is real, sequenced, first-task work, not assumed done.
- **The Env-1/Env-2 hard wall is a SEPARATE, pre-existing undertow-internal invariant this plan must
  respect, not create.** `docs/master-roadmap.md:20` states: *"ZERO kernel-framework types in any
  `Undertow.*` signature/field"* — this refers to Unity's Burst/Jobs/DOTS concept (undertow's own term,
  predates Yeroket), NOT Yeroket's `com.yeroket.utility.kernel-framework` package. Same string, unrelated
  meaning — do not conflate them when reading undertow's docs. The wall permits kernel-framework/Unity
  types only in `Undertow.Bridge`/`Undertow.View` ("ENV-2 stubs"), never in the authoritative `Undertow.*`
  core (`core/` = pure C#, `netstandard2.1`, deterministic, headless). Any generated code this plan
  produces for undertow's C# side must land in the ENV-2-permitted area, never in `core/`'s
  Env-1-authoritative sim types.
- **Target files this plan actually touches** (confirmed exist verbatim, per Inc-3's own doc line
  369-370): `undertow/core/src/Undertow.View/ViewSchema.cs`,
  `undertow/core/src/Undertow.Authoring.Codegen/{ViewWriterGenerator,EmitViewWriter,EmitViewContractHeader}.cs`.

## Scope boundary (what Inc-5 IS and is NOT)

- **IS:** (a) SoA wire-format emit support in the Yeroket kernel-framework's existing `ViewLayout`/
  `ViewWriterEmitter` machinery (Inc-3's `Soa` case, currently a deliberate throw, becomes real); (b) a
  SoA-aware C++ reader counterpart (extending or sibling to Inc-3's `ViewWireReader`/`ViewStore` path);
  (c) declaring undertow's 5 view sections (`Bodies`/`Hud`/`HudFactions`/`HudEvents`/`HudInspect`) as
  `[View]`/`[ViewSection(Layout=Soa)]` schemas — reusing Inc-Ovr's newly-shipped `[Projected]`/
  `[Overridden]` attributes wherever a column's `Source` expression (e.g. `HudFactionFocused`'s
  `el.IsFocused ? (byte)1 : (byte)0`) is a transform rather than an identity bind; (d) regenerating
  `view_contract.h` (VIXEN C++ side, replacing `EmitViewContractHeader.cs`'s hand-rolled emitter output)
  and a generated C# writer (replacing `ViewWriterGenerator.cs`/`EmitViewWriter.cs`'s hand-rolled output),
  both stamped with the Inc-3 `ViewVersionHash` (D4: "replacing undertow's hand-bumped `FormatVersion`");
  (e) preserving the exact `UTVW`/`UTFB` wire bytes undertow's real host/renderer already read/write today
  (a byte-for-byte compatible regeneration, not a new wire format) — this is a REPLACEMENT of the
  generator, not a change to what gets sent over the seam; (f) retiring `Undertow.Authoring.Codegen`'s
  view-specific emitters (`EmitViewContractHeader.cs`, `EmitViewWriter.cs`/`ViewWriterGenerator.cs`) and
  `ViewSchema.cs`'s hand-declared `FormatVersion`/intro-version constants once the schema-driven
  regeneration is proven byte-identical.
- **PROOF vehicle:** the same discipline every prior increment in this program (and the View-Model-Binding
  program) has used — re-derive an ALREADY-SHIPPED, ALREADY-TESTED behavior through the new mechanism and
  prove byte/behavior-identical output, THEN delete the hand-written duplicate. Concretely: declare
  undertow's real `Bodies`/`Hud`/`HudFactions`/`HudEvents`/`HudInspect` schema through `[View]`/
  `[ViewSection(Layout=Soa)]`, regenerate `view_contract.h` + the C# writer, and diff the regenerated
  bytes against what undertow's CURRENT hand-rolled generator produces for a real captured frame (or the
  existing golden tests' fixtures, per `ViewContractHeaderTests.cs`/`SchemaJsonEmitTests.cs`) — exact
  match required before touching anything undertow-side. Only after that match is proven does this plan
  retire the old emitters and switch undertow's actual `.csproj` `Analyzer` reference from
  `Undertow.Authoring.Codegen`'s view slice to the Yeroket kernel-framework CLI's generated output.
- **IS NOT:** migrating undertow's OTHER ~30 codegen emitter files (save codecs, patch parsers, authored-
  kind builders, effect/action/param/system registration, merge logic) — those are a separate, much
  larger, explicitly out-of-scope epic; this plan touches ONLY the view-contract slice named above.
- **IS NOT:** changing the wire bytes/protocol undertow's host (`vixen/app`) and renderer (`vixen/render`)
  actually exchange today — this is a generator-replacement, not a protocol redesign. If the regenerated
  output is NOT byte-identical to what's shipping today, that is a BLOCKER to investigate and resolve
  (likely a schema-declaration mistake on this plan's part), not something to silently accept as "close
  enough" or paper over by bumping the version.
- **IS NOT:** touching anything in undertow's `core/` Env-1-authoritative sim types — all generated/
  consuming code for this migration lands in `Undertow.View`/`Undertow.Bridge` (the existing ENV-2-
  permitted area), per the hard wall documented in `docs/master-roadmap.md:20`.
- **IS NOT:** the dispatch-side kernel migration hinted at by `TagDensityKernel.cs`'s comment (a future
  `[VMKernel]`/Burst-dispatch integration) — that is a different, unstarted, much larger epic (compute
  dispatch, not view-contract codegen) and is explicitly not this plan's concern.
- **Reuse, don't reinvent:** Inc-3's `ViewLayout` core, `ViewWriterEmitter`, `ViewVersionHash`, and the
  `[View]`/`[ViewSection]` attribute family (all shipped, Opus-validated, on both origins) are the
  substrate this plan extends (SoA emit) and finally applies to a real external consumer (undertow) for
  the first time. Inc-Ovr's `[Projected]`/`[Overridden]` attributes (also shipped, this session) are the
  mechanism for any `ViewSchema.cs` column whose `Source` expression isn't a flat 1:1 bind (e.g. ternaries,
  casts, computed values — several columns in the real schema have exactly this shape).

## Tasks

### Task 1 — Ground the shape (READ + REPORT before building)

- Read `ViewSchema.cs` in full (already captured verbatim in this plan's Ground Truth, but re-verify
  file:line against current undertow HEAD before building — this plan is scoped as of undertow `master`
  2026-07-10, VIXEN/Yeroket main as of the Inc-Ovr merges 2026-07-12) and categorize EVERY column by
  whether its `Source` expression is a flat 1:1 bind (candidate for a plain `[View]` field) or a
  transform (candidate for `[Projected]`) — report the categorization for all ~35 columns across the 5
  sections before writing any schema declarations.
- Read `EmitViewContractHeader.cs` and `EmitViewWriter.cs`/`ViewWriterGenerator.cs` in full — understand
  exactly what C++/C# output they currently produce (the "byte-identical" bar this plan must hit).
- Read Inc-3's `ViewLayout`/`ViewWriterEmitter`/`ViewWireReader` source (Yeroket + VIXEN sides) in full to
  understand the AoS emit path this plan extends to SoA — confirm the exact shape of the `NotSupportedException`
  Inc-3 left as the SoA stub, and decide the SoA serialization algorithm (per-column contiguous arrays +
  a shared row-count header, matching what `ViewSchema.cs`'s own doc comment implies) before building.
- Read the `Str`/`ListVec3f` variable-length column handling in `ViewSchema.cs`'s own doc comment (lines
  5-9) closely — these are NOT fixed-size scalars; the SoA serializer must handle a `rowCount+1` offsets
  array + a blob, exactly as documented. Confirm this against how undertow's CURRENT hand-rolled emitter
  actually implements it (`EmitViewWriter.cs`) before designing the schema-driven equivalent.
- Decide the exact regeneration/diff proof mechanism: capture a real frame's current wire bytes from
  undertow's existing (hand-rolled) writer, then compare against the schema-driven regeneration's output
  for the identical input — report how you'll capture that reference frame (a golden fixture already in
  `ViewContractHeaderTests.cs`/`SchemaJsonEmitTests.cs`, or a fresh capture) before building.
- Confirm the Env-1/Env-2 hard wall's exact boundary in undertow's `.csproj` structure (which project is
  `core/` pure-C#, which is the ENV-2-permitted consuming layer) so any new generated-code consumption
  this plan wires up lands correctly, not accidentally introduced into `core/`.

### Task 2 — SoA wire emit (Yeroket)

- Extend `ViewLayout`'s SoA case from a throwing stub to a real emitter, following Task 1's serialization
  decision. Extend `ViewWriterEmitter` to actually produce SoA output when a `[ViewSection(Layout=Soa)]`
  field is declared.
- Regression: Inc-3's existing AoS tests must be unaffected (the AoS path is unchanged; only the
  previously-throwing SoA branch gains real behavior).

### Task 3 — SoA-aware C++ reader (VIXEN)

- Extend or sibling `ViewWireReader`/`ViewStore` to read the SoA wire format Task 2 produces. Prove a
  round-trip (write via the new SoA emitter → read via the new reader → values match) mirroring Inc-3's
  own AoS round-trip proof pattern (`test_view_wire_roundtrip.cpp`).

### Task 4 — Declare undertow's real schema + regenerate + prove byte-identical

- Declare the 5 real sections (`Bodies`/`Hud`/`HudFactions`/`HudEvents`/`HudInspect`) as `[View]`/
  `[ViewSection(Layout=Soa)]` schemas, using `[Projected]` for every non-identity `Source` expression
  found in Task 1's categorization.
- Regenerate `view_contract.h` (C++) and a C# writer, and prove BYTE-IDENTICAL output against undertow's
  current hand-rolled generator's output for the same reference frame (Task 1's chosen fixture) — this is
  the mandatory, non-negotiable gate before Task 5 touches anything undertow-side.

### Task 5 — Retire the hand-rolled undertow generator (the actual migration)

- ONLY after Task 4's byte-identical proof: switch `Undertow.View.csproj`'s `Analyzer` reference (or the
  equivalent build wiring) from `Undertow.Authoring.Codegen`'s view-specific emitters to the Yeroket
  kernel-framework CLI's generated output. Retire `EmitViewContractHeader.cs`, `EmitViewWriter.cs`/
  `ViewWriterGenerator.cs`, and `ViewSchema.cs`'s hand-declared `FormatVersion`/intro-version constants
  (replaced by the Inc-3 `ViewVersionHash`, per D4).
- Update undertow's own golden tests (`ViewContractHeaderTests.cs`/`SchemaJsonEmitTests.cs`/
  `GeneratedHeaderTests.cs`/`SchemaDisclaimerTests.cs`) to validate the NEW generated output rather than
  the retired hand-rolled one — do not simply delete these tests; they encode real correctness properties
  (the `<auto-generated/> DO NOT EDIT` banner discipline, the regen-command convention) that the new
  generator must still satisfy in its own way.
- Live-gate: undertow's actual sim→render seam (real host + renderer, not just a unit test) must still
  function identically — this is the ultimate proof the migration didn't silently change wire semantics.

## Gates / guardrails

- Byte-identical regeneration (Task 4) is mandatory and non-negotiable before Task 5 begins — do not
  proceed to retiring the hand-rolled generator on a "close enough" or "the tests still pass" basis if the
  raw bytes differ even by one byte in a way that isn't explained and justified.
- Every `Source` expression's semantics must be preserved EXACTLY through its `[Projected]` transplant —
  this program's own precedent (View-Model-Binding Inc-Ovr) shows a byte-identical bit-logic re-derivation
  is achievable and is the bar to meet, not "approximately the same."
- No generated/consuming code for this migration may land in undertow's `core/` Env-1-authoritative sim
  types — confirm every new file's project membership respects the existing Env-1/Env-2 hard wall.
- The SoA serializer must correctly handle `Str`/`ListVec3f`'s variable-length offsets+blob encoding
  (`ViewSchema.cs`'s own doc comment) — this is a real complexity, not a flat fixed-stride array; do not
  under-scope Task 2/3 by treating all columns as fixed-size.
- Cross-repo work (Yeroket + VIXEN + undertow — THREE repos this time, not two) needs its own
  worktree-pairing discipline; follow the established convention from prior cross-repo increments
  (config-struct-codegen epic, kernel-unification program, View-Model-Binding Inc-Ovr) rather than
  improvising a new one.
- rtk masks git exit codes — use `/usr/bin/git`, `sha256sum`, `cmp` for evidence in ALL THREE repos. Poll
  long builds actively, never overlap builds of one target.
- Commit in worktrees (pre-blessed per convention). Do NOT push without explicit confirmation all three
  repos' halves are ready to land together (a partial cross-repo push here is worse than usual — undertow's
  `.csproj` change coupling means an incomplete push could leave undertow's build broken against a Yeroket
  main that doesn't yet have the SoA emitter).

## Milestone Map

- [x] **Milestone 1 (Task 1):** ground the shape — column categorization, SoA serialization decision,
  regeneration/diff proof mechanism, Env-1/Env-2 boundary confirmation (report-back gate, no building
  until confirmed). One Sonnet implementer + one Opus validator.
- [ ] **Milestone 2 (Task 2-3):** SoA wire emit (Yeroket) + SoA-aware C++ reader (VIXEN) + round-trip
  proof. One Sonnet implementer + one Opus validator.
- [ ] **Milestone 3 (Task 4):** declare undertow's real schema, regenerate, prove byte-identical against
  the current hand-rolled generator's output. This is the increment's hardest correctness bar — may need
  sub-milestones per section if the categorization/regeneration proves large. One Sonnet implementer + one
  Opus validator.
- [ ] **Milestone 4 (Task 5):** retire the hand-rolled undertow generator, update undertow's own golden
  tests, live-gate the real sim→render seam. One Sonnet implementer + one Opus validator.

## Progress Log

- Milestone 1 (Task 1, research-only): DONE · 2026-07-12 · no files modified in any of the three repos
  - **Column categorization (30 columns, 5 sections):** 22 identity (flat 1:1 bind), 8 transform (7
    bool→byte/enum-cast ternaries + `OrbitParent`'s nullable-unwrap-with-`-1`-sentinel; `OrbitPath`'s
    nullable-unwrap-to-null is a borderline 9th case). Full per-column table recorded in this milestone's
    implementer report (see session transcript) — Milestone 4's implementer must re-derive or request it,
    not re-categorize from scratch.
  - **Corrections to this plan's Ground Truth, verified independently by the controller:**
    1. `EmitViewContractHeader.cs` consumes a `view-schema.json` intermediate
       (`Undertow.View/ViewSchemaJson.cs`'s `Serialize()`), NOT `ViewSchema.cs` directly — confirmed
       `ViewSchemaJson.cs` exists at that path.
    2. Inc-3's "own file" `ViewLayout.cs` was renamed to `ViewWireFormat.cs` during the kernel-unification
       consolidation — `ViewLayout` now names only the `Aos`/`Soa` enum. Confirmed:
       `$KF/SourceGenerator~/Transpiler/ViewWireFormat.cs` exists; no `ViewLayout.cs` file exists.
    3. `SchemaJsonEmitTests.cs`'s actual class (`SchemaJsonFreshnessTests`) pins an unrelated kind-schema
       artifact — the view-schema-specific test is `ViewSchemaJsonTests.cs`; the byte-golden fixture
       (needed for Milestone 3's proof) is in a THIRD file, `ViewContractTests.cs`
       (`ViewBuffer_MatchesGoldenBytes`, diffs against committed `golden/seed7-view.bin`, regeneratable
       via `UNDERTOW_UPDATE_VIEW_GOLDEN=1`).
    4. **`[Projected]` requires a real, separately-authored `[KernelCallable]`-hosted C# method per
       transform column** — confirmed `ProjectedAttribute(Type hostType, string methodName)`'s actual
       constructor (`GpuStructAttributes.cs:53-58`) takes a host type + method name, NOT a free
       expression string. Milestone 4 must author 8 real callables (or fewer if some transforms combine),
       not just apply an attribute — budget real design time, especially for `OrbitParent`/`OrbitPath`'s
       richer nullable-unwrap shape.
    5. **Confirmed SoA throw site verbatim** (`ViewWireFormat.cs:36-37`):
       `if (f.Layout == ViewLayout.Soa) throw new NotSupportedException($"SoA emit is Inc-5+; field
       '{f.Name}' must use Aos for now")` — thrown eagerly at codegen time, not embedded in generated
       runtime code. No `WriteSoaBody` method exists yet. `ViewVersionHash.Compute` hashes only
       name+kind (not layout), so an Aos→Soa flip doesn't change `SchemaVersion` — relevant for
       Milestone 3's byte-identical proof.
    6. **SoA serialization decision (Task 2, confirmed):** per-column contiguous arrays + one shared
       row-count header for fixed-size scalars; `Str`/`ListVec3f` get their OWN
       `(rowCount+1)`-offsets-array + blob PER COLUMN (not one shared per-row blob as in AoS) — reuse the
       existing offsets+blob subroutine from `EmitViewWriter.cs`'s current AoS emitter, just move its
       invocation from "once per row, interleaved" to "once per column, before any row data."
  - **Env-1/Env-2 boundary, confirmed via actual `.csproj` files (corrected by the Opus validator):**
    two "Undertow.View"s exist — `core/src/Undertow.View/Undertow.View.csproj` (plain `netstandard2.1`)
    and `unity/Packages/com.undertow.app/Runtime/View/Undertow.View.asmdef`. **Correction:**
    `docs/master-roadmap.md:20`'s hard-wall exemption ("excluding `Undertow.Bridge` and `Undertow.View`
    which are ENV-2 stubs") names `Undertow.View` BY NAME without distinguishing the core-csproj from the
    unity-asmdef — so the CORE `Undertow.View.csproj` is ALSO in the exempted set, not exclusively the
    unity one as originally framed. This makes the plan's work MORE clearly compliant either way — the
    real compliance guarantee is "consume Yeroket Analyzer-only at build/codegen time, never a runtime
    `PackageReference`," which holds regardless of which `Undertow.View` is considered "the" exempted one.
  - **Milestone 3's proof-fixture gap flagged:** the existing `seed7-view.bin` golden does NOT exercise
    non-empty `Str`/`ListVec3f` columns (a neighboring test notes "no factions/events yet") — Milestone 3
    must extend the golden scenario (or add a second golden) so the SoA offsets+blob path for populated
    variable-length columns is actually exercised before trusting a byte-identical match as sufficient.
  - No blockers. Ready for Milestone 2.
  - **Opus validator (independent re-verification across all three repos):** confirmed the 22/8 column
    split exactly via an independent pass; refined it with a real nuance for Milestone 4 — `Mass`
    (←`el.MassKg`), `TopRelSig` (←`el.TopRelSignificance`), `Cause` (←`el.CauseString`) are value-identity
    (no transform) but their VIEW-FIELD NAMES differ from their C# source-member names, and the
    kernel-framework `[View]` model has no "backing member name" concept (`ViewModelBuilder.Build` derives
    fields from the struct's own field names) — these 3 columns need a name-binding mechanism at
    Milestone 4 despite carrying no value transform; `Mass` additionally hides a `double→float` narrowing.
    Confirmed `OrbitPath` is genuinely as complex as `OrbitParent` (both nullable-ternary), not a lesser
    "borderline" case. Confirmed the `ViewSchemaJson.cs` intermediate, the `ViewLayout.cs`→`ViewWireFormat.cs`
    rename (git-log-verified at the actual renaming commit `51ede47c`), the exact SoA throw site, and
    `ProjectedAttribute`'s real constructor requirement — all independently re-verified against source.
    **Corrected the "8 callables" estimate**: `[KernelCallable]`'s fixed typed signature means ONE shared
    `byte BoolToByte(bool)` callable serves all 4 bool→byte columns, and the 2 enum casts collapse per
    enum type — the real count is closer to 3-4, not 8; the plan's own "or fewer if transforms combine"
    hedge was accurate, just conservative. Walked a concrete 2-row SoA example by hand and confirmed the
    proposed offsets+blob-per-column algorithm is self-consistent and matches `ViewSchema.cs`'s own
    doc-comment. Confirmed undertow's CURRENT `UTVW` wire is ALREADY columnar/SoA (its emitter's
    offsets+blob subroutine is already per-column) — strengthening the plan, since undertow's existing
    output is itself the reference for what Yeroket's new SoA emitter should produce. Confirmed the
    `seed7-view.bin` golden gap is real and sharpened it (a separate round-trip test for populated Str
    exists but doesn't pin bytes — insufficient for Milestone 3's byte-identical bar). One correction to
    the Env-1/Env-2 framing (see above, folded in). **APPROVED, Milestone 2 can proceed.**

## Follow-ups (explicitly out of scope, note for later increments)

- Migrating undertow's OTHER ~30 `Undertow.Authoring.Codegen` emitter files (save codecs, patch parsers,
  authored-kind builders, effect/action/param/system registration, merge logic) — a separate, larger epic;
  this plan touches only the view-contract slice.
- The `TagDensityKernel.cs`-hinted future `[VMKernel]`/Burst-dispatch integration — a different epic
  (compute dispatch), not view-contract codegen.
- View Contract program's remaining faces per the parent design doc: face 2 (authoring reference), face 4
  (view→action, the AppFlow convergence, parent §7) — unrelated to this plan's undertow-migration scope.
- View-Model-Binding Inc-E (authoring tooling / UX API + lint guards) — a sibling program's own remaining
  item, unrelated to this plan.

## Note

This is the first increment in the View Contract Codegen program that actually touches the undertow repo
— every prior increment (Inc-1 through Inc-3) was deliberately scoped to prove the mechanism in-tree
first, per each increment's own "do NOT touch undertow" discipline (D1 in multiple designs). That
discipline paid off: the SoA gap this plan must close was identified and explicitly deferred, not
discovered as a surprise now. Per the program's own established pattern (and View-Model-Binding's, most
recently with Inc-Ovr): the proof is not "the new schema compiles" — it is "a real, already-shipped wire
format is reproduced byte-identically through the new mechanism, and the old hand-rolled generator is
retired, not left running alongside it."
