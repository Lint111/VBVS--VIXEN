{
  "_comment": "Baked-Perf-Fix-Pipeline M2d (Vixen-Docs/01-Architecture/Baked-Perf-Fix-Pipeline-Plan-2026-07.md). Two modes: same_path is a HARD GATE for every milestone from M2d onward (bit-exact virtual==baked is impossible by construction, so this checks a run against its own golden, not the two providers against each other). cross_path is REPORT-ONLY until M5's baked-lighting-parity work closes the known virtual<->baked lighting divergence -- 'enforced' flips true only when M5 marks it so in this file.",
  "same_path": {
    "_comment": "Golden vs a fresh same-provider run (e.g. baked vs baked). Tolerance is <=2 of 625 cells, widened from the initial hash-equality per this file's own documented procedure, WITH the observed run pair cited: 2026-07-16, golden (worktree-binary, HEAD d3a212f6) vs main-binary (merge 0daf4f29) flipped exactly ONE cell -- map row 21, the floor(3)/right-wall(1) grazing-corner near-tie pixel first documented by the M2 validator. Main-binary runs are byte-identical to each other (A/B verified) -- the flip is a stable cross-binary compile difference (FMA/rounding), not run-to-run jitter. bodies_match and oob checks remain HARD requirements; the tamper failure signature (single-body corruption) measured 9/625 cells + bodies_match=False, so <=2 cells cannot mask it. If a future pair exceeds 2 cells, that is a REAL failure -- do not widen again without the same class of evidence.",
    "enforced": true,
    "instidx_max_differing_cells": 2,
    "instidx_total_cells": 625,
    "require_bodies_match": true,
    "oob_count_max_relative_delta": 0.05,
    "luminance_p99_max_delta": 2.0
  },
  "cross_path": {
    "_comment": "Baked vs virtual -- the two providers are expected to diverge today (baked lighting is not yet parity-matched to virtual's). This mode REPORTS the divergence so it is machine-quantified and tracked, but does not fail the gate. M5 (Baked-Perf-Fix-Pipeline-Plan-2026-07.md Milestone M5) flips 'enforced' to true once baked-lighting-parity closes -- do not flip this by hand without that milestone's sign-off.",
    "enforced": false,
    "instidx_agreement_pct_target": 90.0,
    "require_bodies_match": true,
    "luminance_mean_abs_delta_budget": 40.0,
    "luminance_p99_delta_budget": 120.0,
    "pct_pixels_over_threshold_budget": 60.0
  },
  "pixel_diff_threshold": 24,
  "instidx_grid_size": 25
}