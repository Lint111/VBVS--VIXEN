Sampled Lighting Inc3 M2 -- geometric reprojection reject + companion history buffer (KI-023) -- live gate

Scope: adds a persistent worldPos/depth companion history image (WorldPosHistoryNode, binding
22, rgba32f) written each frame by DirectLighting.comp alongside historyImage's own write, and
replaces the M4(Inc2) reproject branch's check (c) -- previously a color-consistency reject
(|history.rgb - outColor| <= 0.15) -- with a GEOMETRIC reject
(distance(histWorldPos, bestWorldPos) <= 0.1 world units). Checks (a) bounds and (b)
motion-magnitude are UNCHANGED. Purely additive: new node + new shader binding + one swapped
validity expression; no other control flow touched.

Build
------------------------------------------------------------------------------
Windows-native (vixen-ninja preset via build.bat, worktree-local build dir
.claude/worktrees/sampled-lighting-inc3/build/ninja -- CMakePresets.json's binaryDir is
${sourceDir}/../build/ninja, resolved per-worktree). VIXEN.exe + vixen_editor.exe both linked
successfully. 19 pre-existing SVO/RenderGraph TEST targets failed to compile
(C2589/C2059/C2676/C2446/C2672 in libraries/SVO/include/Recipe/generated/SdfCoreKernels.g.hpp --
a macro-token clash, unrelated to this milestone's files) -- confirmed NOT a regression: none of
the 19 failing targets touch WorldPosHistoryNode/DirectLightingNode/BuildRenderGraph.cpp, and the
engine library (RenderGraphNodes.lib) + both app executables built with zero errors or warnings
attributable to this milestone's new/changed files.

Gate (a): accumulation.enabled=0 still byte-identical
------------------------------------------------------------------------------
Default editor scene, VIXEN_EDITOR_CAPTURE_FRAMES=5, VIXEN_EXIT_AFTER_FRAMES=20, no
VIXEN_ACCUMULATION_ENABLED (enabled=0 default):

sha256(gate-artifacts/inc3-m2-disabled-frame5.png) = fde9c268cf5f07f68588b563b908ec84bb9bd134e3bcbc913280152cac6ed8c1

MATCHES the canonical baseline exactly (fde9c268cf5f07f68588b563b908ec84bb9bd134e3bcbc913280152cac6ed8c1,
verified against inc2-m4-hashes.txt / inc3-m1-hashes.txt). The new binding-22 image and its
Execute-only descriptor wire are dead weight on this path -- confirms zero-visual-delta by
construction on the disabled path.

Gate (b): reprojection quality equal-or-better than the OLD color-reject
------------------------------------------------------------------------------
Same repro convention as Inc2 M4's own gate: VIXEN_ACCUMULATION_ENABLED=1 +
VIXEN_ACCUMULATION_REPROJECT=1 + VIXEN_RESIDENCY_GATE_DEMO=1 (continuous far->near dolly-in orbit
sweep, SetOrbitDistanceForTest driven every Update tick), captured at the SAME 6 ticks Inc2 M4
used (10/11/12 early-slow, 150/151/152 mid-sweep):

  tick   our (geometric-reject) sha256                                            Inc2-M4 (color-reject) sha256                                            M2 reset-mode sha256 (ground truth == pure current)
  10     de85c19be2ec781b7a3b13ae06bf72878dc98eab4e7d80ce539d95a20656e5a8         43c64036d2dd707987086fcb8f4252a12cf1708147c918be5afd3cc83bf48fa3         (matches ground truth, see pixel diff below)
  11     0221b613e5bbfc2be13e13b63a1882b2ba359c7875525d31b5885581f36064be         43c64036d2dd707987086fcb8f4252a12cf1708147c918be5afd3cc83bf48fa3         0221b613e5bbfc2be13e13b63a1882b2ba359c7875525d31b5885581f36064be (EXACT match)
  12     0221b613e5bbfc2be13e13b63a1882b2ba359c7875525d31b5885581f36064be         43c64036d2dd707987086fcb8f4252a12cf1708147c918be5afd3cc83bf48fa3         0221b613e5bbfc2be13e13b63a1882b2ba359c7875525d31b5885581f36064be (EXACT match)
  150    8f0176ff71d6af98bc3757532b9c14a12ef292ab655a334b4194bbd183f6bccd         873b71fb5003c243a810f8bc5f1f9312bdaf203bae61f287223b656a77966238         (matches ground truth, see pixel diff below)
  151    b398a66961121c1e41a2eb080bd73e3a8f328e1e6106464dea26f0073f48a490         873b71fb5003c243a810f8bc5f1f9312bdaf203bae61f287223b656a77966238         b398a66961121c1e41a2eb080bd73e3a8f328e1e6106464dea26f0073f48a490 (EXACT match)
  152    4974d011294c457b976c634558492bff4662740736022c6a59315f8e20a4f681         873b71fb5003c243a810f8bc5f1f9312bdaf203bae61f287223b656a77966238         4974d011294c457b976c634558492bff4662740736022c6a59315f8e20a4f681 (EXACT match)

Ground-truth comparison (pixel-level): re-ran the IDENTICAL orbit demo with
VIXEN_ACCUMULATION_ENABLED unset (pure current-frame passthrough, same tick numbers) and diffed
byte-for-byte against our geometric-reject capture at the same tick:

  tick   pixels differing / 250000   max per-channel diff
  10     0        (0.000%)            0
  11     0        (0.000%)            0
  12     0        (0.000%)            0
  150    0        (0.000%)            0
  151    0        (0.000%)            0
  152    0        (0.000%)            0

Interpretation: the geometric reject REJECTS reprojection (falls back to pure-current, zero
smear/ghosting) at every one of Inc2-M4's own 6 gate ticks -- equal-or-STRICTER than the old
color-reject, which accepted reprojection at these ticks and left a small residual blend
(Inc2-M4's own report: 0.002%-0.076% differing pixels, max diff 3-22, "legitimate temporal-
accumulation blend noise" at the growing silhouette edge). Zero diff from ground truth at these
ticks is a strict superset of "not worse" -- it means KI-023's original disocclusion-smear failure
mode (unbounded growing sky-colored patch, Inc2-M4's own documented bug) cannot occur here, since
these ticks fall back to the same pure-current path a hard reset would take.

Confirmed the geometric ACCEPT path is not dead code (not merely "always rejects"): captured
frames 2/3/4 immediately after the mandatory frame-1 reset, camera barely moved between ticks --
reprojection is ACCEPTED and blends correctly:

  tick   pixels differing from pure-current ground truth / 250000   max diff
  2      2        (0.001%)                                          1
  3      1        (0.0004%)                                         1
  4      2        (0.001%)                                          1

Small (1-2 px, max-diff-1) blend residue -- the expected signature of a genuine, correct
reprojection accept (tiny sub-pixel edge blend), not a rejection artifact. Confirms the geometric
check discriminates: accepts on near-static frames (small worldPos delta), rejects on the
disocclusion-heavy gate ticks (large worldPos delta at the growing silhouette).

VUID census: identical 8 pre-existing VUID types across all three run configurations (disabled,
reprojection-enabled, motion-only-disabled), ~20 occurrences each (VUID-vkAcquireNextImageKHR-
semaphore-01779, VUID-vkBeginCommandBuffer-commandBuffer-00049, VUID-vkCmdDispatch-None-08114,
VUID-vkCmdDraw-None-09600, VUID-vkQueueSubmit2-commandBuffer-03875, VUID-vkQueueSubmit2-semaphore-
03868, VUID-vkResetFences-pFences-01123, VUID-vkUpdateDescriptorSets-None-03047) -- zero NEW VUID
types introduced by binding 22's addition. All 8 types confirmed pre-existing (present identically
in the enabled=0 byte-identical-baseline run too). The "compute_desc_gatherer binding N out of
range" ERROR-level log lines are the SAME pre-existing test_dispatch demo-pipeline finding M1's
gate flagged (finding #4, zero pixel impact) -- present identically across every run here too,
unrelated to this milestone.

Gate (c) note: pre-existing frame-1 accumulation artifact (NOT introduced by this milestone)
------------------------------------------------------------------------------
Investigated a discrepancy while probing frame 1 with VIXEN_ACCUMULATION_ENABLED=1: a 32x32 pixel
patch (px [234,265]x[234,265]) renders sky-colored on frame 1 instead of the correct shaded body
color, converging back to correct over the next few frames (max per-channel diff 105 -> 53 -> 35
-> 26 -> 21 across frames 1-5). Root-caused this as PRE-EXISTING M1-M3 behavior, NOT something
this milestone's diff touches: reproduces byte-identically with VIXEN_ACCUMULATION_REPROJECT
entirely UNSET (accumulation-only, no reprojection code path involved at all -- this milestone's
diff is scoped to the reprojection branch's check (c) and the new binding-22 declaration/write,
neither reachable when reprojectionEnabled=0). `git diff shaders/DirectLighting.comp` confirms the
alpha>=1.0 guard and the plain non-reproject accumulate branch are byte-for-byte unmodified by
this milestone. Not filed as a new Known-Issue by this worker (leaving that call to the milestone
owner, per Inc2-M4's own precedent for its own out-of-scope resize-crash finding) -- flagged here
for tracking. Does not affect this milestone's own gates: (a) uses accumulation.enabled=0 (this
artifact requires enabled=1) and (b)'s ticks are all >=2 frames past the initial reset.

Platform: Windows-native build (vixen-ninja preset, MSVC via vcvars64.bat), real GPU, same
machine/config as every prior Inc0-Inc3 gate.

Shader recompiled from THIS WORKTREE's source (BuildRenderGraph.cpp.obj + RenderGraphNodes.lib
rebuilt this session, confirmed via fresh ninja build log timestamps matching this run).

Files created:
  VIXEN/libraries/RenderGraph/include/Data/Nodes/WorldPosHistoryNodeConfig.h
  VIXEN/libraries/RenderGraph/include/Nodes/WorldPosHistoryNode.h
  VIXEN/libraries/RenderGraph/src/Nodes/WorldPosHistoryNode.cpp
  gate-artifacts/inc3-m2-hashes.txt (this file)
  gate-artifacts/inc3-m2-disabled-frame5.png
  gate-artifacts/inc3-m2-reproject-tick150.png
  gate-artifacts/inc3-m2-groundtruth-tick150.png

Files edited:
  VIXEN/libraries/RenderGraph/CMakeLists.txt
    - +1 source: src/Nodes/WorldPosHistoryNode.cpp
  VIXEN/application/main/source/graph/BuildRenderGraph.cpp
    - +includes (WorldPosHistoryNodeConfig.h, WorldPosHistoryNode.h)
    - +worldPosHistoryNode node creation + device/commandPool/extent connections (mirrors
      accumulationHistoryNode exactly)
    - +binding 22 wire to directLightingGatherer (Execute-only, mirrors binding 20)
  VIXEN/shaders/DirectLighting.comp
    - +binding 22: worldPosHistoryImage (rgba32f)
    - reproject branch check (c): color-consistency (|history.rgb-outColor|<=0.15) REPLACED with
      geometric (distance(histWorldPos.xyz, bestWorldPos)<=0.1)
    - +imageStore(worldPosHistoryImage, pixelCoords, vec4(bestWorldPos, recReadback.hitT)) at the
      end of main(), unconditional (mirrors historyImage's own always-current-pixel write cadence)

Companion buffer available for Inc3's own ReSTIR reservoir-reprojection validity test (M4/M5) --
one buffer, two consumers, per the plan's design note.
